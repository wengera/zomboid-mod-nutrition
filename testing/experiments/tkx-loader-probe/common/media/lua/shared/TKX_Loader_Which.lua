-- COLLIDING RELATIVE PATH: media/lua/shared/TKX_Loader_Which.lua exists in BOTH common/ and
-- 42.20/. Whichever tree the loader resolves last wins the global, and the loader prints an
-- `overrides` line for the pair. This copy is the common/ arm.
TKX_LoaderWhich = "common"
