-- UNIQUE to common/: no file of this relative path exists under 42.20/, so nothing can
-- override it. If this global is missing, the common/ tree never ran at all -- which is a
-- different fact from "the common/ tree lost the collision".
TKX_LoaderCommonTree = 1
