-- UNIQUE to 42.20/: no file of this relative path exists under common/, so nothing can
-- override it. If this global is missing, the 42.20/ tree never ran at all -- which is a
-- different fact from "the 42.20/ tree lost the collision".
TKX_LoaderVersionTree = 1
