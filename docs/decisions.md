# Decisions ledger

Every judgment call taken unattended goes here (spec: "take the default, log
it"). Each entry is reversible: overturn it by editing the line and, if work
depended on it, adding a ripple note in [progress.md](progress.md).

| Date | Slice | Decision | Alternatives | Why |
|---|---|---|---|---|
| 2026-09-09 | program | Slices are question-driven and sized for one session; plans written just-in-time per wave | one slice per phase; source-driven sweeps | cold-start finishability and crisp acceptance; sweeps folded in as tooling where cheapest |
| 2026-09-09 | program | Wiki pages fetched with `curl` + browser user-agent from the raw/API endpoints | in-app browser; user pastes pages | the plain fetcher is blocked (403); raw wikitext carries the wiki's own page-version stamp |
| 2026-09-09 | program | Fixture world uses `Zombies=6` (none) | vanilla zombie population | deterministic, safe spawn area for unattended runs; zombie-dependent behaviour is out of scope for nutrition |
| 2026-09-10 | program | Slices execute on `main` directly (no worktree) | feature branch + worktree | the golden fixture blobs are gitignored and exist only in this checkout; the approved protocol is commit+push per slice |
| 2026-09-10 | program | All subagents run on Opus; the lead stays on Fable | cost-tiered models per task | user instruction |
| 2026-09-10 | program | Research-only subagents (notes, mirrors) may run beside one implementer and never commit; the lead commits their output | strict serialization | no file overlap, and withholding git from them removes index contention |
| 2026-09-10 | 01 | `doc_lint` acceptance per slice = 0 findings in the dirs the slice owns/touches; pre-existing findings elsewhere are deferred to the slice that owns those docs | repo-wide zero from slice 01 | keeps slices bounded |
| 2026-09-10 | 01 | `doc_lint` resolves paths against the repo root (`--root` for tests), lints file targets directly, skips `.superpowers/`; `sources` must have a non-blank line before the next `## ` | brief's original root-relative design | the original gave false greens on file/path arguments |
| 2026-09-10 | 01 | The pre-refresh Modding hub excerpt is kept as `references/wiki-mirrors/modding-hub-archived.md` | delete it (page slug changed) | mirrors exist to preserve drift; the page's 42.20.x news section is gone from the live page |
| 2026-09-10 | 01 | Harness `eat` keeps its client-side `AddItem` fallback (experiment-only, reports `spawned`); anything that must stay error-free spawns server-side via RCON `additem` | remove the fallback / baseline the NPE | the NPE is a real vanilla symptom of client-created items and must not be masked; `pzt run` never uses `eat` |
| 2026-09-10 | 01 | No live re-run for a fix round that only reorders teardown/writes; the next live task validates it | re-run the smoke (~2 min) | the matrix run exercises the same code path minutes later |
| 2026-09-10 | 01 | Measured-evidence JSON (matrix/smoke results) is committed under `testing/artifacts/<run-id>/`; docs cite those paths | keep results only in gitignored `testing/runs/` or `.superpowers/` | an M grade must be checkable from a clone; the files are small |
| 2026-09-10 | 01 | Harness `eat` uses the 3-argument `IsoGameCharacter.Eat(item, fraction, useUtensil)` (works from Lua); recorded in the command's `signature` field | 2-argument form | the 3-arg form is what `ISEatFoodAction` calls |
| 2026-09-10 | 01 | Drink-type test item is `Base.HotDrink` (HungerChange 0, ThirstChange −20, not a `Test*` item) | any other `food.txt` drink | first match of the plan's criterion |
| 2026-09-10 | 01 | Sandbox `Nutrition` flipped at runtime via `getSandboxOptions():set(name, value)` (worked first try; the `getOptionByName(...):setValue` fallback and the `sendToServer` push path are untested) | re-provision with `--sandbox Nutrition=false` | runtime flip was enough for the probe |
| 2026-09-10 | 01 | The n=1 observation that a client-local sandbox flip coincided with the server's drain slowing is recorded only as an open question (slice 03 re-tests with an explicit push and a no-flip control), not as an M row | record it as measured | `SandboxOptions.set` is client-local per bytecode; one run without a control cannot establish the mechanism |
| 2026-09-10 | program | When a fix round follows a slice's last live run, the artifact README must note the script/artifact skew (producing commit + what the current script adds) instead of assuming a later run erases it | re-run the experiment after every fix round | re-runs for teardown-only changes are poor value; disclosure keeps the evidence honest |
| 2026-09-10 | 02 | All measured steps of the slice run as one live session script (`s02_lifecycle.py`, phased saves) instead of four scripts | one script per plan task | one boot instead of four; per-phase saves keep earlier evidence if a late phase wedges |
| 2026-09-10 | 02 | Claim-level fixes after the last live run are not re-run; the artifact README discloses the script/artifact skew | re-run the session (~6 min) | the fixes changed wording and a prediction denominator, not measurements |
| 2026-09-10 | 02 | The doc task ran in parallel with the measured task's scoped re-review, working from the controller-corrected brief | strict serialization | disjoint files; the corrections were already known |
| 2026-09-10 | 02 | `cooked` is graded C (not M) in the ItemStats packet table: both sides already read `false`, so agreement was uninformative | grade it M with burnt/cookingTime/heat | only diverging fields carry information |
| 2026-09-10 | 02 | `ItemStatsPacket` item-state field count is stated as 39 (isCustomName is applied to the item via `setCustomName`, so it is state, not a header flag); the doc names both counting conventions | 38 (isCustomName as a presence flag) | the doc's own criterion — applied on receipt — decides |
| 2026-09-10 | 02 | The cross-item `cookingTime` leak is documented from code (write skips zero fields, apply is unconditional) with at most one confirming live run | three discriminating live runs | the mechanism is C-gradable; the hazard covers every conditionally-written field |
| 2026-09-10 | 03 | All body-side rates are sampled on the SERVER (`stats.get`, `nutrition.get`), the client only as a mirror witness | client-side sampling | slice 01 established that the client neither drains hunger/thirst nor owns weight; a client sample would measure the 1 Hz packet, not the mechanism |
| 2026-09-10 | 03 | Tasks 1+2 (code reading) ran as one dispatch into `03-notes.md`; Task 3 as one live session; the doc task last | one dispatch per plan task | the shape that worked for slice 02: one boot, one artifact, one code map |
| 2026-09-10 | 03 | Research started while the slice-02 doc and its re-review were still running | strict slice serialization | slice 03 depends only on 01 and its reading tasks touch no shared files |
| 2026-09-10 | 03 | One live session: idle baseline at 1x for >= 300 s (fixes S), every other condition at 30x in >= 20 s windows, scale-free ratios and worldAge-based rates; ~1 Hz Python polling of an atomic server `stats.get` replaces the planned Lua `stats.sample`/`stats.sampler` scheduler; movement rows only where a simple automation worked | the Lua sampler; one session per condition | an atomic snapshot removes clock/value skew (r^2 1.000000 on calories over 233 samples); slice 04 built the scheduler anyway; `player.run` never registered as running on the server, so the running row is n/a |
| 2026-09-10 | 03 | New client commands live in `client/PZTestKit_Client_Body.lua` | edit `PZTestKit_Client.lua` | the slice-02 fix wave was editing that file at the time |
| 2026-09-10 | 03 | No targeted re-run for the asleep row (lost to a bus rename race) and the 50-65 band edges (weight drift while the row ran); both graded C with the mechanism and a re-run recipe in the doc | a 3-minute re-run | the live slot was held by the slice-04 3-day runs; the rows are not load-bearing |
| 2026-09-10 | 03 | Post-run claim fixes (wording, comments, poisoned-key guards) were not re-run; the artifact README discloses the skew and lists the keys not to cite; the doc task ran in parallel from the corrected brief | re-run; serialize | disjoint files; the mechanism text was already known to be wrong |
| 2026-09-10 | 03 | The FOOD_EATEN timer is documented as a game-time rate (`minutesPerDay/30` units per game-second: 3.0 on the 90-minute fixture, 2.0 on a 60-minute day), not a frame-rate one | keep the per-real-second reading | re-derived from the artifact: 3.00000 units per game-second across every interval while the per-real-second figure drifts with the multiplier |
| 2026-09-10 | 03 | The health-loss rows (`body-stats.md` HUNGRY == 4 mechanism and the rejected "dying of thirst" wiki claim) are left to the slice-04 doc task: the 3-day run killed a subject whose hunger never reached level 4, and a jar re-read is in flight | patch the wording tension now | one coherent edit from the jar beats two edits to the same rows |
| 2026-09-10 | 03 | The `pzt run --hold 5` acceptance is folded into the next live run after the slice-04 harness edits land, together with one `nutrition.applytraits` call (the only rewritten harness body never executed live) | run it now | the live slot must not boot while another implementer is editing harness Lua |
| 2026-09-10 | program | Wave-2 plans (05, 06, 07) were drafted by three parallel Opus planners from a shared brief and the finished slices, then reviewed and amended by the lead (one shared script parser: slice 05 ships `parse_script` with `lines`; slice 06 imports it and never adds a second walker) | lead writes every plan by hand; plans written only after wave 1 closes | the planners verified the install (no `media/scripts/fluids/`, B42 drinks are `normal` items with a `FluidContainer` component, 969 `craftRecipe` blocks, zero legacy `recipe` blocks) while wave 1 was still in review; the lead keeps the cross-plan interfaces consistent |
| 2026-09-10 | 04 | Scenarios run SERVER-side (test layer in `shared/`, scenarios under `server/scenarios/`, `test.run` on the server bus, results in the server cache); `pzt scenario --side server\|client` defaults to server; the attached client stays a live player and mirror witness | the plan: client-side scenarios sampling the client player | slices 01/03 established that the server owns Nutrition and weight; a client-side `setCalories` is overwritten within a second |
| 2026-09-10 | 04 | Daily intake fed as two +2000 doses 12 game-hours apart; the evaluator integrates the model over the hourly server samples against the predicted weight, tolerance max(15 % of the predicted delta, 0.05 kg) | one +4000 dose (clamped to 3700); compare against the measured weight | the calorie store clamps at 3700; integrating against the prediction keeps the check a model test rather than a one-step derivative test |
| 2026-09-10 | 04 | The gain run pins hunger and thirst each hour; the unpinned first run is kept as evidence (subject died at game-hour 35) | discard the failed run; feed real food | `updateWeight` and `updateCalories` read neither hunger nor thirst, so the pin is a control, not a distortion; the death is the finding that exposed the THIRST branch |
| 2026-09-10 | 04 | Tasks 1+2 built as one dispatch while the slice-03 live session ran (the implementer polled `pzt doctor` for the slot); Task 3 ran in parallel with the T1+T2 review; the T1+T2 fix round waited for the 3-day runs to finish | strict serialization | the runs were the long pole; editing the test layer under a running scenario risked the second run |
| 2026-09-10 | 04 | `pzt doctor` counts only LISTENING/ESTABLISHED local sockets (TIME_WAIT from the previous run false-failed the port check for ~2 min); netstat state names assumed English | wait out TIME_WAIT | folded into the T1+T2 fix round; zero cost |
| 2026-09-10 | 04 | A dead subject fails a scenario run (`dead_any` in the verdict); `cadence_suspect` (EveryOneMinute outside 0.95–1.05 ticks per game-minute) stays advisory | pass on tolerance alone; fail on cadence | a late death could pass inside the tolerance; the model check integrates over worldAge deltas, so a slow clock does not bias it |
| 2026-09-10 | 04 | The Task 3 review's THIRST attribution was settled on the jar by a fresh implementer (the branch exists: `BodyDamage.Update @1320–@1358 L2377–L2379`, `/10`); the body-stats.md / 03-notes.md corrections were applied by the slice-03 final fix wave rather than the slice-04 doc task | apply them from slice 04 | one writer per file; the slice-03 wave was already editing body-stats.md |
| 2026-09-10 | 04 | The three scenario artifacts (tree at 707c878) are kept as-is after the harness changed (dd84a37, ef64241); the artifacts README discloses the skew and the offline re-evaluation (FAIL / PASS / PASS, no stored key changed) | re-run the three 3-day scenarios (~30 min) | the changes are numerically inert on the existing evidence; the next scenario run exercises the new `thirst` column and the dead-subject verdict |
| 2026-09-10 | 04 | The doc task review and the whole-branch final review ran in parallel, with one consolidated fix wave (19 items) | strict task review → final review → fix | the doc task was the last task; the wave was verified by a scoped re-review plus a live `smoke_clock` run (`scenario-20260910-074456`) |
| 2026-09-10 | 04 | `pzt scenario` adopts `pzt run`'s environment rule: non-baseline server errors, client Lua errors and missing mods fail the RESULT (one shared `session.fault_reasons`; `faults` recorded in reports and new artifacts) | keep the scenario verdict on the harness pass + evaluator only | a scenario that passes its model check on a server logging errors is not evidence; the run command already applied the rule |
| 2026-09-10 | 04 | The scheduler's world-clock reads go through a guarded helper (a nil clock is logged once and the clock keeps ticking); Core's own bare `getGameTime()` stays as the documented fail-loudly exception | leave the reads bare | inside the `EveryOneMinute` handler a bare failure kills the scheduler and surfaces only as a runner timeout |
| 2026-09-10 | program | Fix rounds go to a fresh implementer carrying the fix brief and the prior report (the session cannot resume a finished subagent) | resume the original implementer | tool availability; the cost is a little re-reading per round |
| 2026-09-10 | 05 | No reusable script parser exists, so slice 05 writes a nesting-aware line-loop parser in `tools/food_scan.py`, shared with slice 06 | import or copy `pz-b42`'s `insulation_scan.py` / `vehicle_scan.py` / `entity_sprites.py` | all three flatten or chunk instead of nesting (a `component FluidContainer`'s `Capacity` merges into the item's own keys), none is importable from a clone, and one reads `ItemName_*.txt`, a layout B42 no longer ships |
| 2026-09-10 | 05 | Four buckets, first match wins — `food` 722, `drainable` 150, `fluid_container` 133, `fluid` 61; drainables included | food only (only 2 drainables carry `Calories`, both `0.0`) | drainables are the other half of the 114-key union and slice 06 joins recipe results across every kind; the three item rules are measured disjoint on 42.20.4, so the ordering is a guard against a mod rather than a live tie-break |
| 2026-09-10 | 05 | CSV curated, JSON complete: the CSV is 59 declared columns plus `source_file` / `source_line`, the JSON adds `props_raw`, the whole `fluids` records and `meta` | one format; a CSV column per script key | the CSV is for reading and joining by hand and the JSON is the machine source of truth; a column per key would be ~114 mostly-empty columns, and nested blocks would have to be flattened — the thing this slice rejected the existing parsers for |
| 2026-09-10 | 05 | The harness ships `fluid.script <id>` beside the spec's `items.count`, keyed by `getFluidTypeString()` with a `getFluidType()` fallback and a `fluidTypeRoute` field in the reply | `items.count` alone; key fluids by `getFluidTypeString()` only | a count cannot check fluid *identity* or the nutrition join, and the string getter names only 34 of the 61 definitions — every enum-backed fluid (`Water`, `Beer`, `Coffee`, …) answers it empty |
| 2026-09-10 | 05 | An absent script key is the empty string in the CSV and `null` in the JSON, never `0`; a key that really writes `0` still prints `0` | `0` for an absent key | absence is load-bearing (225 blocks declare no `DaysFresh`, `Base.Salt` no macro key) and reads back as the engine's own default, which is `1000000000` for the thresholds — confirmed live in both directions on `Base.RatKing` (`0 / 0`) and `Base.CannedCorn` (absent) |
| 2026-09-10 | 05 | Every `Block` carries `entries` (every `Key = Value` line in file order) beside `props` (last write wins) | `props` only; make every key a list | 3 512 of 133 705 prop lines are repeats `props` cannot see — 145 `fluid =` lines over 68 `Fluids` blocks, 34 `SoundMap` lines in `drainable.txt` — while `props` still mirrors what the loader does for a scalar key |
| 2026-09-10 | 05 | A block header is decided by lookahead: a non-`Key = Value` line whose next logical line is `{`, with `kind` the first token and `name` the rest of the line | the two-token `(\S+)$` regex, with an anonymous `?` block for an unmatched brace | 156 shipped blocks have a multi-word or numeric name (`evolvedrecipe Stir fry`, the vehicle templates' `1` / `2`) and were becoming `?` / `?` and mis-filing their header into the parent's `lines`; after the change all 34 683 blocks parse with 0 anonymous opens |
| 2026-09-10 | 05 | An int-typed key coerces int → float → str: an integral float literal becomes an int (`UnhappyChange = -10.0` → `-10`), and only a value neither parse accepts stays a raw string | keep the float; truncate always | the fluid files put float literals on 141 int-typed values and every one is integral, so one `KEY_TYPES` entry keeps one Python type and one CSV column keeps one type; a genuinely fractional literal keeps its float rather than being silently truncated |
| 2026-09-10 | 05 | Task 3 owns the row schema: the column list, its order and the empty-vs-zero rule live in `COLUMNS` and `data/README.md`, and a *declared* column is typed by the dataset while a pass-through field is typed by `coerce` | type everything through `coerce` | `Capacity` is a component key and `KEY_TYPES` is the item table, so `coerce` would leave `"0.3"` a string; the same rule is why a fluid's `alcohol` stays raw instead of being mapped onto `alcohol_power`, which no doc supports |
| 2026-09-10 | 05 | `replace_on_deplete` is column 48, appended after `weight` | insert it after `replace_on_use`, as column 43 | the fix brief's two placement statements cannot both hold — inserting it renumbers seven already-published columns — so the checkable number won and columns 1–47 stayed where they were published (renumbered to 59 by fix round 2's eleven columns) |
| 2026-09-10 | 05 | Task 4 (harness) ran beside Task 3 (dataset), and Task 5 (live run) beside Task 3's fix round | strict serialization | disjoint files — `testing/` and `docs/testing/` against `tools/` and `data/` — with each implementer staging only its own paths; the one shared risk, which dataset bytes the live run read, is disclosed as a *do not cite* row in `testing/artifacts/README.md` rather than left implicit |
| 2026-09-10 | 05 | Plan Q3 (fluid nutrition per unit or absolute) was answered by a read-only research pass running in parallel with the implementation | answer it inside Task 3; defer it to slice 06 | it is a jar read with no file overlap, and the answer changed the dataset schema — much better known before Task 5 measured against the dataset than after |
| 2026-09-10 | 05 | The per-unit decision point fired and was taken as: keep the raw per-litre values, add `nutrition_basis`, the four fill columns and six `*_per_container` columns (raw × `fluid_fill_litres`), and do **not** apply the `/100` | the plan's single `calories_per_container`; overwrite the raw columns; divide by 100 | `nutrition_basis` is what stops a consumer summing apples and colas; six twins make the arithmetic auditable; and keeping script units on both sides makes a can's `-3.6` hunger directly comparable to an apple's `-16` |
| 2026-09-10 | 05 | A live drink probe was added beyond the plan (Task 5b, harness `drink`) to close slice 01's open question 11, and its outer-bracket band was **not** widened after three calorie rows missed it | ship the fluid path C-only; retro-fit the tolerance to the rows that failed | one 2-minute session takes the per-litre arithmetic to C+M on two fluids, two capacities and two fractions; the three misses are 0.002–0.008 kcal of the idle-burn model's already-measured 0.4–0.7 % excess, and tuning a band to the data it failed on would make the verdict meaningless |
| 2026-09-10 | 05 | `data/food-items.csv` carries **no** build stamp; the pair's JSON `meta` and [`data/README.md`](../data/README.md) are what document it (the spec's "each file carries the game build it was generated from" is read as per pair) | a stamp/comment row above the header, as the spec's wording reads literally | any line above the header breaks `csv.reader` and every spreadsheet import, which is the whole point of the curated half; the JSON twin is written in the same run from the same `meta`, so the provenance is one file away and cannot disagree |
| 2026-09-10 | 05 | `nutrition_source` (and `nutrition_basis`) are **empty** on the 280 rows that carry no nutrition key on either side — `705a12f` | keep `food_keys` on them, as the first build did | a row with no nutrition key has no source to *name*, and `food_keys` there reads as "these columns came from the item's own keys" rather than "there is nothing here"; the wider set is 290 rows carrying no nutrition **value**, the extra 10 being `per_litre` rows joined to a fluid with no `Properties` block or only `alcohol` |
| 2026-09-10 | 05 | The Task 5b review and the Task 6 doc ran in parallel, and their findings landed as one consolidated final fix wave together with the whole-branch review's | serialize: 5b review → fix → doc → doc review → fix | every 5b finding was documentation-level — the measurement stood, and the comparator at HEAD was replayed offline against the committed artifact to prove it (72 fields, 69 matched, the same three flagged rows) — while the doc task wrote only `docs/`; one wave also keeps `testing/artifacts/README.md`, the file both would have touched, under a single writer |
| 2026-09-10 | 06 | Slice 06 ships a **separate** `tools/recipe_scan.py` that imports `food_scan`'s parser (`sys.path` insert + `import food_scan`), and `tools/food_scan.py` is byte-unchanged | a recipe module inside `food_scan.py`; a second walker | the plan's architecture rule (05 owns the DSL parser, 06 owns the recipe schema); the parser needed no change, so slice 05's item path and its 46 tests were never at risk |
| 2026-09-10 | 06 | `itemMapper` pairs are read from a block's **`lines`** (one `partition("=")` in `recipe_scan._mapper`), leaving `food_scan.PROP_RE`'s identifier-only rule alone | widen `PROP_RE` to accept dotted keys so the pairs land in `props` | measured first: the change moves 1 599 mapper lines into `props` and touches 0 lines slice 05 reads — but it collapses the 56 mappers that name one result twice down to their last source, and contradicts the shipped module's own docstring. `itemMapperPairs` keeps every line in file order instead |
| 2026-09-10 | 06 | `consumed = mode != "keep"` on every IO line — no `mode:` and `mode:destroy` both consume | treat only `mode:destroy` as consuming | `ItemUser.UseItem @28 L37-38` skips the reduction only when `keep` is true; the plan briefed the same rule |
| 2026-09-10 | 06 | An input amount `N` is a count of **uses**, not items, unless the line carries `flags[ItemCount]`; for a `Food` one use is one raw `HungerChange` point, and a `mode:destroy` line charges the **whole item** with the remainder published as `delta.destroyWaste` | the plan's `N × item macros`; charge `N` uses on a destroy line and ignore the annihilated rest | the jar read (`.superpowers/sdd/06-recipes/q-itemcount-notes.md`) and then the measurement (`exp06b-20260910-120123`, 96/96 fields): `item 10 [Base.Icecream]` really is a third of a tub. **Ten** of the 31 deltas moved in at least one macro (seven of them exact-`InheritFood` splits to 0), 21 did not; `ScoopIceCream` went from −16 345 to −105 kcal. `destroyWaste` keeps both readings recoverable rather than picking one |
| 2026-09-10 | 06 | A food input with **no usable `HungerChange`** is charged as a one-use item (with a `notes` entry naming `Food.getMaxUses`'s `baseHunger == 0 ? 1`), and only the **exact** flag `InheritFood` marks a split — `InheritFoodAge` does not | the fix brief's literal wording: refuse such a delta (`no-hunger-scale`), and treat `InheritFood*` as a glob | both readings are forced by the brief's own worked examples: refusing would have dropped four correct deltas (`OpenCandyPackage`, `OpenHotdogPack`, `PackCigarettes`, `open_mac_and_cheese`), and the glob would zero `ScoopIceCream`, which must be −105. `InheritFoodAge` copies age only (`createOutputItems @1192–@1311 L1550–L1562`) |
| 2026-09-10 | 06 | A delta term is taken only from a `nutrition_basis == per_item` row: a `per_litre` row refuses with `fluid-sourced`, an empty basis with `no-nutrition`, and fluid IO is recorded verbatim, flagged `fluidIO` and excluded from the macro sum | convert a fluid's per-litre nutrition into the delta with the container's capacity | slice 05's decision that a drink's nutrition is per **litre** and lives on the fluid, not the item; converting here would sum two units and duplicate slice 05's fluid model inside slice 06. The refusal is by name, so it is checkable |
| 2026-09-10 | 06 | Where the plan's predicted figures did not reproduce, the **measurement wins** and both numbers are recorded in `docs/vanilla/recipes-dataset-notes.md` § Discrepancies: 116 → 375 recipes touching food, 23 → 15 (+5 splits +3 absent), 10 → 11 + 26 output-less, 187 → **189** Salad ingredients, and the craftRecipe-scoped `mode:`/`flags[…]` counts | adopt the plan's numbers; drop the plan's numbers silently | the plan's own decision point ("a different number is a finding, not a failure") and the live `recipes.count` / `getPossibleItems()` arbiter; `meta.counts.pairsFromFoodTxt` (6 858) and `componentCraftRecipes` (202) keep every superseded figure reconstructible from the data |
| 2026-09-10 | 06 | Task 4 (the live cross-check) ran **before** Task 3 (the evolved dataset), so its five `recipes.evolved` replies were recorded verbatim and compared against nothing; Tasks 3 and 5 own that verdict | hold the live slot until the evolved dataset existed | the live slot was free and the craft half was ready; the artifact's `meta.sequencing` and its README block say so, and the comparison was then made offline against the committed artifact (five lists, set-equal both directions) |
| 2026-09-10 | 06 | `FOOD_FIELDS` maps the plan's script-case names onto `data/food-items.json`'s lowercase typed columns, and every food read goes through `food_value(row, name)`, which falls back to the raw script key | rename the plan's contract; read the dataset's columns directly | a non-identity mapping was the plan's own decision point; the fallback keeps both shapes working, so the plan's verbatim fixtures (keyed `Calories`/`HungerChange`) and the shipped rows both resolve through one reader |
| 2026-09-10 | 06 | Evolved rows: 21 duplicate (recipe, item) joins collapse to **one** row keeping the **last** key written (the loader's map is last-write-wins), with the collapsed keys in `duplicateKeys`; `thirstSkipped` flags only the **27** rows where the `DRIED_FOOD` skip actually removes a thirst the item carries (not all 577 tagged rows); `hungerClamped` stays on a spice row | emit both joins as separate rows; flag every tagged row; drop the clamp flag where hunger is already 0 | every one of the 21 repeats the same `use`, so no number is at stake and a duplicate row would double-count a consumer's sum; the flags answer this library's standing question — *is this 0.0 absent, measured or substituted?* — and the clamp flag records that the **key over-asks**, which is still true of a spice |
| 2026-09-10 | 06 | The per-use rule was measured with a new harness command `item.use` (Task 4b, beyond the plan), running `item:setCurrentUses(cur − used)` because `ItemUser` is not exposed to Kahlua; removal-on-depletion stays grade **C** | ship the rule C-only from the jar; call `InventoryItem.Use()` N times | that fallback **is** the line `UseItem @28 L37-38` executes, and it is the only path by which crafting reaches hunger; N `Use()` calls would be N separate roundings. What it skips (the `replaceOnUse` spawn, `sendItemStats` while uses remain, and `RemoveItem`) moves no nutrition field, and the experiment computes its removal expectation for the route that actually ran rather than papering over it |
| 2026-09-10 | 06 | The evolved contribution applies **three branches the plan's verbatim formula omits** — `addItem`'s hunger clamp (`@934 L374-376`, 59 rows), the `Spice = true` early return (`@582–@775 L336–L359`, 2 522 rows) and the `DRIED_FOOD` thirst skip (`@1316 L416`, 27 rows) — each with a per-row flag; only the rotten branch is left unmodelled | apply the plan's block verbatim and note the divergences in the doc; apply the branches silently | fidelity to the loader is the whole point of a reference dataset: without the clamp the file published a Cooking-10 share of 1.0 where the game gives 0.7, without the spice return a hunger of `use/100` the dish never spends, and without the thirst skip water the pot never receives. The flags (`hungerClamped` / `spice` / `thirstSkipped`) are what keep the plan's simpler formula reconstructible from the data |
| 2026-09-10 | 06 | `delta.destroyWaste` is `null` unless one of the six macros it would carry is **non-zero**, so `meta.counts.recipesWithDestroyWaste` is 0 — the one `mode:destroy` round-up (`UnpackCigarettes`) wastes a drainable with no macro keys, and its round-up is recorded in `delta.notes` instead | publish the six zeroes (what the first build did); count the round-up rather than the payload | a six-zero block reads as a measured "0.0 kcal destroyed" when the truth is that the destroyed row carries nothing this dataset can weigh, and `nutrition_delta`'s own docstring promised null when nothing was wasted. The rounding is not lost: `notes` names it, and on that row also says that `mode:destroy` supersedes the drainable rule |
| 2026-09-10 | 06 | The combined scanner re-review (fix rounds 1+2) and Task 5 (the notes doc) ran **in parallel**, and their findings — with the whole-branch review's — landed as one consolidated final fix wave | serialize: re-review → fix → doc → doc review → fix | the doc had to cite the final data, and the re-review's own finding was that both rounds were correct, so the parallel pair cost one doc re-edit against roughly an hour of serial waiting. One wave also keeps the files both would have touched (`testing/artifacts/README.md`, the notes doc, the plan) under a single writer, and every driver/harness change it made is disclosed as script skew rather than re-run |
| 2026-09-10 | 07 | The acceptance subject is **KeenPerception** (workshop `3685392864`): 15 KB, one shared Lua file, no `require=`, 0 `mod_lint` findings | `FasterResting` `3634568288` (the plan's fallback); a mod written for the purpose | spike S3 already proved it loads under `-nosteam`, and the shipped `trait.check` reads its *effect* back on both sides — so the run proves the mod ran, not merely that the server did not complain. The fallback never fired: both probes passed on the first attempt |
| 2026-09-10 | 07 | `copy = false` is the one knob that reproduces the missing-mod path | uninstall a real mod for the run; point the profile at a bogus workshop id | it names the mod in `Mods=` and places nothing, so the regression needs nothing installed and changes no machine state; `enable_client_mods` still lists the id, so the client walks the same road as the server (S3-A) |
| 2026-09-10 | 07 | `[sandbox]` overrides are **merged** into the restored fixture's own `SandboxVars.lua` (`merge_sandbox_vars`), never replaced with a partial table | keep `write_sandbox_vars` on every path, as the pre-slice code did | a partial write silently resets the fixture's whole 189-key file — its provisioned `Zombies = 6` among them. Measured: after the server's own boot rewrite the run's copy carries the profile's `DayLength = 1` **and** the fixture's `Zombies = 6`, at 189/184 keys, one line of `diff` — read off `testing/artifacts/run-20260910-133657/pzt_SandboxVars.lua`, the run's own post-rewrite file, committed for this row (the run's `report.json` holds no sandbox data) |
| 2026-09-10 | 07 | `PZTestKit` is prepended (first in `Mods=`) to any profile that does not list it, and the run logs the resulting list | fail a profile that omits it; let it run harness-free | no harness = no command bus = no ready marker, no `[[verify]]` probes and no `pzt run` at all; a profile that genuinely must run without it is a later, explicit `harness = false` key |
| 2026-09-10 | 07 | A `[sandbox]` key that is not a **settable top-level** option of the fixture's own file is a hard error before boot (with the three closest names) | accept it and let the merge append it | an appended key is dropped by the server's boot-time rewrite, so the run would report an option it never applied; nested options (`Map.*`, `ZombiesDragDown`) and table openers are out of scope for this slice — a profile needing one re-provisions with `pzt provision --sandbox` |
| 2026-09-10 | 07 | `tools/mod_lint.py` stays standalone — paths and workshop ids only, no `--profile` mode, no import of `testing/pzt` | teach it to lint the mods a profile names | `tools/` stays runnable without the game, the fixture or a live session (L0 is the CI-able layer); the profile loader already points a failure at `python tools/mod_lint.py <src>`, which is the same information from the side that has the path |
| 2026-09-10 | 07 | `WorkshopItems=` is written **empty** under `-nosteam`; a profile's numeric workshop id survives as provenance in the file and in `report.json` | seed the ini with the profile's workshop ids | S3: both workshop roots are gated on `SteamUtils.isSteamModeEnabled` and the setting needs Steam, so only the copy into `<cachedir>/mods` loads anything; the clients' `WARN:MISSING in SettingsTable: WorkshopItems` for the empty value is baselined noise |
| 2026-09-10 | 07 | `mod_lint`'s `mod-info-place` rule **assumes** B42 reads the newest version folder's `mod.info` first; the assumption is recorded as an open question rather than presented as a finding | accept a `mod.info` in any `42*` folder, as `pzt/mods.py:mod_id_of`'s glob does | the two readings disagree on 6 installed mods, and on `3774052732/SD_CC_TEST` they would disagree about the mod's *id*; the rule is a WARN (never a run-failing ERROR) precisely because it is unsettled, and slice 12's mod-anatomy doc settles it from the engine |
| 2026-09-10 | 07 | Where the plan's predicted corpus figures did not reproduce, the **sweep wins** and both numbers are recorded with their dates in `docs/testing/profiles.md` § L0 | adopt the plan's 1 ERROR + 6 WARN; drop the plan's numbers silently | measured 3 ERROR (the plan missed `id-agree` on `SD_CC_TEST`, having compared only the resolved `mod.info` per folder) and 30–31 WARN; and the corpus is a **live tree** that moved mid-slice — a Steam update to item `3490370700` moved one `media/` WARN between the Task-1 sweep and the doc's re-run hours later, so a count is only quotable with its date |
| 2026-09-10 | 07 | Tasks 1–3 were dispatched **in parallel** on disjoint files (`tools/` vs `testing/pzt/` vs the CLI), and Task 1's `tools/README.md` entry was deferred to the doc task | serialize the three; let Task 1 write its own README entry | the three touch no shared file, and the live acceptance (Task 4) could not start until all three had landed anyway; `tools/README.md` is the doc task's file, and one writer per file is the standing rule |
| 2026-09-10 | 07 | `docs/testing/profiles.md` carries a hand-written `Verified against:` stamp and C/M/W grades although `docs/testing` is **not** in `doc_lint.STAMPED_DIRS`, which this slice does not widen | widen `STAMPED_DIRS` to `docs/testing` so the new doc is machine-checked | widening flags **2 files / 4 findings** — `doc_lint.SKIP_FILES` skips any `README.md`, so it is `pipeline-design.md` and `spikes.md` that would need stamps and grades, not the three the plan named — still a cleanup of its own, and one this slice's files do not all own; the doc still reads like the rest of the library, and its grades were checked against the rule by hand |
| 2026-09-10 | program | Wave-3 plans were drafted by two parallel Opus planners from a shared brief while slice 07 was in its final review; slices 09–11 share ONE template plan (`09-11-teardowns.md`) executed once per mod, with slice 08 supplying the picks (LongTermPreservation4220 → simpleStatus → AutoCook, fall-through recorded) | one plan file per teardown; plan after wave 2 fully closes | the three teardowns are the same procedure on different subjects; slice 08 (the picks, the witness commands) is the only real dependency and its inputs were committed |
| 2026-09-10 | 07 | The `profile` timeline mark carries `skip=` (the ids named in `Mods=` that `copy = false` places nowhere) | leave it out; print it only when non-empty | `harness.install` drops those ids from `missing_mods` on purpose, so the run log would otherwise never say that a later `mods_not_found` was *intended* — the one line that separates a deliberate missing-mod regression from a broken profile. Printed always (`skip=none`) so its absence is never ambiguous |
| 2026-09-10 | 07 | **Finding, recorded as a caveat rather than fixed**: `DayLength = 1` × `--speed 30` overruns the game-minute scheduler (`ticks_per_world_min: 0.22`, `cadence_suspect: true` on `scenario-20260910-134012`). The rule of thumb is `24 × speed / day_minutes ≲ 8` | raise the harness's event ceiling; refuse a profile whose `DayLength` × `--speed` exceeds the budget | the ceiling is the GAME's `Events.EveryOneMinute`, which the harness only subscribes to — nothing in this repo can raise it. Refusing the combination needs a ceiling number, and 10.08/s (that run) is the only measurement there is, against 8/s as the highest demand known sustainable. So it is documented at every site a timed scenario would be templated from, and `cadence_suspect` already fails loudly on the run itself |
| 2026-09-10 | 07 | The missing-mod fail-fast runs on the **plain** `pzt run` / `pzt scenario` path too, and `scenario.write_artifact` records `profile` (null without one) and `verify` (empty) on every run | gate both on `--profile`, keeping the pre-slice-07 shape for a profile-less run | a fixture's own recorded `Mods=` can stop resolving exactly as a profile's can — a mod uninstalled since provisioning — and the game's answer is still a WARN and a clean boot (S3-A), so the cheap check is worth as much there. A constant artifact shape is worth more than a shorter one: a reader never has to ask whether a missing `profile` key means 'no profile' or 'an older writer' |
| 2026-09-10 | 07 | `server_started` / `client_ready` pass their step's duration as **`took=`**; `t` is elapsed time on every mark | leave `t=` overwriting the elapsed value and keep a read-trap note in the docs | one key of shape change against a trap that had already been read wrongly once: `t: 37.3` on `client_ready` was cited as 'client ready at 37.3 s' when the run reached in-world at ~74 s. Fixed at the source so future artifacts need no note; the three slice-07 artifacts and everything older carry the old shape and say so in `testing/artifacts/README.md` § Script/artifact skew |
| 2026-09-10 | 07 | `pzt run` / `pzt scenario` clear the argparse defaults of `hold` / the two timeouts / `safemode` / `launcher` to `None`, and `session.opt` falls back to `profile.DEFAULTS` when neither a flag nor a profile supplies a value | keep 'explicit means differs from the default', which required `profile.DEFAULTS` to equal the argparse defaults | argparse cannot report whether a flag was typed, and the old proxy silently swallowed a flag typed AT its default (`--hold 5` lost to a profile's `hold = 9`). Clearing the defaults makes `None` mean 'not given'; the `PROFILE_DEFAULTS` fallback is what stops a plain `pzt run` handing `hold=None` to `session.hold` and dying inside a session it has already paid a minute of boot for |
| 2026-09-10 | 07 | `scenario.run` runs the profile's `[[verify]]` probes after `client_ready`; a failed probe does **not** abort the run, it folds into the result as `FAIL: verify <cmd>` after the test | abort at the probe, as the fail-fast does; leave the scenario path probe-less | the cost logic differs from `pzt run`'s: there the probes guard a hold that has not happened yet, so a failure skips it; here the ten minutes of test are the thing that was bought, its evidence is written either way, and the verdict is what says the numbers may not be cited. Without this a scenario artifact could only ever evidence that the mods LOADED |
| 2026-09-10 | 07 | `pzt.mods.mod_id_of` sorts its `42*/mod.info` glob by parsed version tuple, and `mod_lint`'s `id-agree` ERROR is scoped to the files a resolver can reach, with an `id-drift` WARN for the rest (9 rules, not 8) | leave the string sort and let `id-agree` cover every `mod.info` in the tree | the string sort put `42/mod.info` above `42.20/mod.info` and read a different file on 6 installed mods — harmless only because all 6 declare the same id, which is what `id-agree` exists to stop being luck. Once the two readers agree, an ERROR on a copy no resolver can open (`LEGACY/42.12/mod.info`, five Frockin Splendor mods) is noise: it becomes `id-drift`. Corpus outcome unchanged — 84: 3/30/51 at 2026-09-10 15:09, 0 drift |
| 2026-09-10 | 07 | A profile that is ambiguous is rejected pre-boot rather than resolved by precedence: `[client] users = []`, a boolean `expect`, `copy = false` on the harness mod, and `path` + `workshop_id` on one entry are all `ProfileError`s | pick the sensible reading for each (fall back to the fixture's clients; stringify the boolean; drop the harness; prefer `path`) | every one of those readings is defensible and none is guessable from the file, which is the definition of a silent surprise in a module whose whole point is that a bad profile costs a second instead of a boot. `users = []` in particular reads as 'server only' and would have meant 'the fixture's clients' |
| 2026-09-10 | 08 | The three teardown subjects are `SKITTLE_LongTermPreservation4220` (item `3774789651`, slice 09) → `simpleStatus` (`2867431511`, slice 10) → `AutoCook` (`3388721641`, slice 11), with the fall-through order `SkillRecoveryJournal` (`2503622437`) → `MoodleFramework` (`3396446795`) → `SomewhatTraitsCore` (`3498347699`) | rank by size or by lint cleanliness; take the plan's list unexamined; pick the most on-topic Workshop item (`3736275816` ApocalipseBR - Nutrition Sync Fix) | six criteria applied in order — installed locally; touches the nutrition surface directly; B42-ready on this build; MP behaviour worth measuring; small enough to read whole in a 2 h teardown; not already torn down — and what is left ranked by the fourth. Criterion 1 removes all six on-topic Workshop items, 5 removes `CleanUI` and `QuestSystem`/`Economy`, 6 removes `BeyondTen`. LTP is the only nutrition candidate whose **only** version folder is `42.20` (`version_dirs == ["42.20"]`; `MoodleFramework` and `ZVirusVaccine42BETA` also ship one, beside others) and it holds both mechanisms at once (15 live item blocks, 14 food, **and** a server-side `OnCooked` hook multiplying four macros and `HungerChange` by 0.70 while pinning `offAge`/`offAgeMax` — fields `ItemStatsPacket` does not carry); `simpleStatus` is the pure client reader (12 nutrition reads, its live folder now 100 % client); `AutoCook` is the cooking-pipeline hook set and the corpus's sharpest `common/` merge case. No fall-through was used ([nutrition-mods.md](mods-survey/nutrition-mods.md) § The three picks) |
| 2026-09-10 | 08 | The client's S6 round-trip `witness.moddata` is renamed **`witness.sync.moddata`**; the shared reflective command keeps the spec's name `witness.moddata` | give the new shared command a different name from the spec's; register both and let one shadow the other | `shared/` loads before `client/`, so a client registration of the same name silently shadows the shared one **on the client only** — the side the reflective witness most needs to answer on. The rename costs one Lua line and two Python call sites (`testing/pzt/spikes.py:154,157`); `kind = "moddata"` on the wire, the `OnServerCommand` comparison and the `witness_moddata_<key>.json` result file are unchanged, and no committed artifact depends on the command name |
| 2026-09-10 | 08 | `data/mod-inventory.json`'s `mod_id` is the id `mod_lint` resolves, and is **`""`** when no `mod.info` exists anywhere (1 row); the folder name is kept beside it as `mod_id_fallback` | fall back to the folder name, as the field did before this slice; drop the record | a folder name silently standing in for an id is exactly what produced the 20 wrong rows this slice corrected, and `""` is what the engine's own resolver sees. Identity is `mod_lint`'s single answer rather than a second opinion (`resolve()` imports `version_dirs`/`info_chain`/`read_info`/`media_root`). Consumers must stop assuming a non-empty id — carried as a ripple |
| 2026-09-10 | 08 | `doc_lint.STAMPED_DIRS` is **not** widened to `docs/mods-survey`; the catalog's stamp, `Ev` grades and `## Sources` are written by hand and checked mechanically by the acceptance one-liner instead | widen it so the new file is machine-checked by the default lint | widening adds 4 findings on `approved-modlist.md` and `teardown-template.md`, neither of which this slice owns — the same ruling slice 07 took for `docs/testing`. The one-liner (`STAMPED_DIRS=('docs/mods-survey',)` over the single file) gets the mechanical check without moving the repo lint; widening for real is a cleanup for whoever owns those two files |
| 2026-09-10 | 08 | Teardown picks come only from the **installed** corpus; a Workshop-only candidate is catalogued, graded **W**, carries its unblocking action, and is never picked | subscribe to the most relevant Workshop items and pick from those | a mod that is not installed cannot be linted, profiled, booted or measured from this repo; the workshop tree is read-only here and Steam needs a human. The action is recorded per row (`subscribe to <id> in Steam, let it download, re-run tools/mod_inventory.py`), so a human can promote one deliberately rather than the sweep doing it silently |
| 2026-09-10 | 08 | Steam Workshop pages are recorded as **extracted facts** — id, title, the two or three `detailsStatRight` stats, plus URL and fetch date — in `data/workshop-search.{json,csv}` and in the catalog's Sources, never mirrored verbatim | mirror them the way `wiki_mirror.py` mirrors PZwiki, which is STRATEGY rule 3's form | a Workshop page is ~90 KB of React-rendered HTML with per-deploy obfuscated class names, not wikitext: a verbatim mirror would be unreadable and would rot on the next Steam deploy, and `wiki_mirror.py` is a PZwiki tool. Logged here as the deviation from the mirror-and-digest form that it is; the browse page yields only the `filedetails/?id=N` href and the thumbnail `alt`, and `--compressed` is mandatory or every regex misses |
| 2026-09-10 | 08 | `tools/workshop_search.py` is a **folded-in source sweep** (spec § Structure — sweeps are folded in as tooling where cheapest), not unplanned tooling | hand-collect the outward half of the catalog; skip it and catalogue only what is installed | the catalog's second question — which B42 nutrition mods exist that this machine does not have — has no offline answer, and a hand sweep of eight terms is neither repeatable nor datable. The tool is stdlib + `curl` in `wiki_mirror.py`'s shape, subscribes nothing and writes nothing under the workshop root; it paid for itself by finding `3736275816` and `3796644824` |
| 2026-09-10 | 08 | The `--details` pass read item pages for **9 of the 180** rows (the 3 installed plus the six ids the catalog names); the ration is **disclosed per row, not modelled**, and no allowance figure is quoted anywhere | fetch all 180 item pages; fetch none; keep the "~13 reads per window" figure the first report asserted | the browse half is complete and the catalog only ever quotes named items, while a literal full pass is ~30 min of paced fetching against an allowance nobody here has measured. The asserted figure was contradicted by the tool's own four dated observations (2026-09-10 15:45 / 15:58 / 16:25 / 16:55), so it was struck and a test pins its absence; a later `--fill` pass read six more with 0 failures (15 of 180 at 16:56), which is itself the evidence that no fixed ration is known |
| 2026-09-10 | 08 | Row state is its own field: `details_status` is one of `fetched` / `not_requested` / `failed`, and `error` is a fetch failure only (`null` on all 180 rows) | keep the overloaded `error` string, which read `details not fetched` on 171 rows | a decision was travelling in a failure field: the CSV read as 171 failures when nobody had asked for those pages. Split, `--fill` can re-ask exactly what failed and `--fill --include-not-requested [--fill-ids …]` can top up the rest, each pass appending `{at, ids, fetched, failed}` to `meta.fill`. The 180 rows were re-encoded, not refetched — `meta.fetched` stays 2026-09-10 16:26 |
| 2026-09-10 | 08 | Item modData readings in slices 09–11 must (a) name the **side** they were read on, (b) exclude `customName` from any mod-key census, and (c) assume a field sync that carries modData **wipes and replaces** the receiver's table | treat item modData as one table with an MP ownership rule; report the cross-side difference itself as the finding | measured on 42.20.4: the same apple `#199691187` answers `getModData() == {}` on the server and `{"customName": "Apple"}` on the client. The mechanism is `InventoryItem.setCustomName`, which unconditionally `rawset`s `customName` into modData and has **six** call sites — `InventoryItem.load`, `SyncItemFieldsPacket.processClient`/`.processServer`, `CraftRecipeData.createOutputItems` and two corpse paths — so it is vanilla deserialization bookkeeping, not mod data; and `SyncItemFieldsPacket.processModData()` calls `getModData().wipe()` on the receiver before copying the sender's keys. `testing/artifacts/exp08-20260910-152944/witness-probe.json` (n = 1) |
| 2026-09-10 | 08 | A zero in a `mod-inventory.json` `stats` / `signals` bucket is trustworthy **only** when the record's `media_at` does not list `common/media` (177 of 230 rows do); no merge rule is asserted by the data or the tooling | assert the wiki's `common` + closest-version merge rule and scan accordingly; use `live_media` alone as the guard | the scan reads the live version folder only, and `live_media` is the narrower fact (24 rows blank end to end) — 111 of the 153 rows that *do* have a live `media/` still have a zero bucket that `common/media` actually holds, 135 of 177 counting the blanks. The merge rule is open question 1 of the catalog: the Mod_structure mirror states it, and AutoCook's live folder (no event registration and two `require`s that resolve only under `common/`) is an inference toward it, not a measurement. Measured 2026-09-10: no `common/media` in the corpus carries a nutrition key, so nothing is hidden from the catalog |
| 2026-09-10 | 08 | `script_item_blocks` is an exact parse of `item <Name>` lines but counts **every** item definition — clothing and vehicles included (288 for `Horse`) — so food items for the picks are counted **by hand** off the `Type = Food` blocks | make the field food-only; quote it as a food count | the field answers "how much script does this mod ship", which is what the catalog needs for sizing; a food filter already exists in `tools/food_scan.parse_script`. Measured while hand-counting: the field then did not strip `/* */` comment blocks, which the engine and `food_scan` both do — overturned by the final-wave row below (9f551f6 strips them), so LTP is **15** live items (14 food) not 17 and `ZVirusVaccine42BETA` is **86** not 102 (881 not 899 across the nine script-signal mods). Recorded in the catalog § Discrepancies row 1 / § Open questions Q2 and in `tools/README.md` rather than fixed: `tools/*.py` and `data/` were closed to this slice after Task 1 |
| 2026-09-10 | 08 | Both nutrition signals scan the **live version folder only** (`<live>/media/**`), which drops `63Type2Van` from the script-signal list — 9 mods, not the plan's 10 | scan every version folder, as the plan's wider figures did | a mod may ship the same scripts in three folders and the build loads one: `63Type2Van`'s nutrition keys live in a B41 flat root `media/` that B42 never reads (B42 signal 0, B41 signal 7), and scanning everything read `ZVirus` at 72 keys instead of 36 and `69mini` at 21 instead of 7. A per-mod record has to describe what the build would actually load, which is also why `bytes` is the one field that still spans the whole folder |
| 2026-09-10 | 08 | Task 2 was finished by a **second** implementer while the first was stopped (`TaskStop`); the finisher re-ran the sweep once with the hardened tool and owns the committed dataset | wait for the original agent to resume — it had already woken twice; keep its 15:52 output and document the gaps | the first agent stopped three times, and its 15:52 dataset was invalid: 177 of 180 detail rows were silent `null`s from an alternate item-page template Steam serves with HTTP 200 (the tool now checks every body for the markers its parser needs). It then woke a **third** time and rewrote `data/workshop-search.json` while the finisher was already running, which is why it was stopped rather than left idle — one writer per file. Re-running cost ~6 min of network and makes the committed dataset the product of a single tool version |
| 2026-09-10 | 08 | Slice 08's live probe was **not re-run** after its review round edited the driver; the artifact keeps its numbers and its README block opens with the skew note | re-run the probe so the committed artifact matches the current script | the standing script/artifact skew rule (2026-09-10, program). Every path the fix round touched was inert on that run — all six `moddata_*` replies arrived as tables, `summary.reasked_probes` is empty, no getter list is near `TK.WITNESS_MAX` — so no number in the file moves, and the three corrections the block carries are re-readings of the file against the 42.20.4 jar rather than new measurements |
| 2026-09-10 | 08 | The **client-side** item modData census (`witness.moddata item:<user>/<fullType> *` on the client) is deferred to slice 09 pass 1 rather than re-run in slice 08 | re-run the live probe with the extra census; leave the gap undisclosed | it is one probe inside a session slice 09 pays for anyway, against a whole boot here. Until it runs, the client half of the cross-side finding rests on `item_client.fields.getModData` alone — so `moddata_item.keyCount`/`.keys` is a *do not cite* row in the artifact README, corroborating the **server** side only |
| 2026-09-10 | 08 | `getSlicesOfBreadEaten` is struck from the plan's player-getter list: 42.20.4 has no such member anywhere, and its landing in `missing` is the witness working rather than a narrowed Lua surface | record it as a Kahlua narrowing of a jar member; keep it in the 09–11 getter lists | a full constant-pool sweep of the jar answers `no class contains that literal`, and `IsoGameCharacter` (1450 methods), `IsoPlayer` (574) and `Nutrition` (26) have no `slice`/`bread` member between them; `media/lua` and `media/scripts` have no hit either. So it behaves exactly like the control name `getNoSuchThing`, and `missing` means "not on this object on this build" — it never distinguishes "the jar has it but Lua cannot see it", which only a jar check does (`IsoPlayer` has no `getCalories` either; it is on `Nutrition`) |
| 2026-09-10 | 08 | `witness.moddata global:<name>` keeps its route through `ModData.getOrCreate` — reached by the new `TK.callStatic`, because the binding is a **dot** call — and its empty census is documented as evidence of the binding and nothing else | the plan's expected default: record `global:` as "not exposed on 42.20.4" and have 09–11 read global modData through a mod-specific command | the plan's expectation was wrong. `getOrCreate` is a static returning a `KahluaTable` (jar-verified) and it answered live with no `error`. What it does is **create** the table when it is absent, so a global census can never report "no such table": the empty `keys` on `pzt_probe_table` is the probe's own table, which is why it is a *do not cite* row. A global census means something only with the mod loaded and run |
| 2026-09-10 | 08 | `tools/mod_inventory.py` **imports `food_scan._strip_comments`** and runs all three script regexes over the comment-stripped file; `sandbox_options` also reads `common/media` | keep a local `re.sub` pair in `mod_inventory`; leave both as they were and keep quoting the corrections in prose | "what the engine ignores" must have one definition in this repo, and `parse_script` (slice 05) already owns it — the same delegation `resolve()` makes to `mod_lint` for identity, and `tools/` may import `tools/`. The payoff is measurable: `script_item_blocks` now equals `parse_script`'s own block count on **all nine** script-signal rows, where it disagreed on two. Regenerated once and rerun byte-identically; 13 rows moved (11 `sandbox_options` false→true, LTP 17→15 items / 135→117 keys, ZVirus 102→86), corpus 6648→6630 |
| 2026-09-10 | 08 | The nine catalogued mods' Workshop "last updated" dates come from a new **`--catalog-ids`** pass writing `data/workshop-catalog-details.json`, one pass, 2026-09-10 17:46 (9 fetched / 0 failed) | widen the sweep's terms until the nine appear; force them into `--details-ids`; leave the column "not fetched" | eight of the nine are returned by none of the eight terms, so `--details-ids` rejects them by name (deliberately — an unknown id would break `requested == fetched + failures`) and `--fill` has no row to fill. Widening the terms would change the trend-sorted row set and `meta.fetched` with it. A separate file keeps the sweep's census honest: no join, no `terms`, no `grade`, and a per-row `fetched_at` because there is no browse pass to inherit a stamp from. Cross-check: the page's File Size equalled the sum of the item's mod-folder `bytes` on all nine, to the byte |
| 2026-09-10 | 08 | Flag combinations `workshop_search.py` cannot honour are `ap.error`s, not quiet no-ops — `--details-ids` without `--details`, `--fill-ids` without `--include-not-requested`, `--fill` beside `--details`/`--details-ids`, `--catalog-ids` beside any of them, `--out` without `--catalog-ids` | let each parse and do nothing, as they did; infer the intent (imply `--details` from `--details-ids`) | on a read that may not land, a silent no-op costs a whole pass and looks like a Steam refusal. Same class as `select_detail_ids`'s unknown-token error, which the tool already raised. Inferring intent is the worse half: `--fill --details` names two different passes over two different inputs and there is no honest guess between them |
| 2026-09-10 | 08 | Slice 07 open question 6 (is `harness.install`'s folder→id rename exercised?) is settled by **pick 1**, not by `simpleStatus`, and the question is recorded as having two halves | keep `simpleStatus` as the named check, as `profiles.md` had it | `SimpleStatus` vs `simpleStatus` differs by case only. NTFS is case-preserving, so a run does answer *which name was written* — that half `simpleStatus` can settle — but Windows path lookup is case-insensitive, so the mod loads either way and the run says nothing about whether the rename is **required**. `LongTermPreservation4220` → `SKITTLE_LongTermPreservation4220` differs by a whole prefix, so it answers both halves, and slice 09 runs it first. No prior decisions row named `simpleStatus` for this, so only `profiles.md` § Open questions 6 and the board ripple needed correcting |
| 2026-09-10 | 08 | The parked `witness.moddata` scope-gate defect is **recorded in the tracked docs and left in the Lua** for slice 09 pass 1 | fix the one-line gate in this wave; leave it in gitignored notes only | the harness Lua is measured-against code: `testing/artifacts/exp08-20260910-152944` was driven by the shipped file, and editing it in a docs-and-tools wave would put every row of that artifact into script/artifact skew for a defect no probe in it exercised. Leaving it undocumented was the worse option — it lived only in gitignored notes — so `docs/testing/README.md` § Command bus carries the disclosure beside the gate bullet and `docs/progress.md` carries the ripple, and slice 09 pass 1 closes it while the harness is live |
| 2026-09-10 | 09 | Pass 1's subject is **`SKITTLE_LongTermPreservation4220`** (item `3774789651`), taken from slice 08's committed order (`bb418a4`, [nutrition-mods.md](mods-survey/nutrition-mods.md) § The three picks); passes 2 and 3 stay `simpleStatus` (`2867431511`) then `AutoCook` (`3388721641`) | the template plan's own fallback queue, whose third slot is `MoodleFramework` (`3396446795`); re-rank the picks at the pass | the plan's Step 1 says take the first pick in slice 08's stated order with no teardown yet, and slice 08's list is the measured one; the two lists differ only in the third slot and nothing here disturbs it. No skip rule fired (`mod_lint 3774789651` → 0 ERROR, 1 INFO `folder-id`), no fall-through was used, and **no scoping ruling was needed**: the subject is 89 lines of Lua across 6 files plus 44 text files, every one read end to end |
| 2026-09-10 | 09 | All three `[[verify]]` rows are **tier (a)** — bus probes reading the mod's own state: server `items.count`, server `recipes.craft Skittles.MakeCuredMeat`, client `item.script Skittles.CuredPork`. The recipe row must be **module-qualified** | tier (b), grepping the mod's own console print; tier (c), the copied folder plus an empty `mods_not_found`; the bare spelling `MakeCuredMeat` the plan's example used | a clean boot is not evidence the mod loaded (spike S3-A), and LTP ships script definitions readable at join, so tier (a) is available — all three passed first time (`run-20260910-191842`). The bare spelling cannot resolve: `ScriptBucketCollection.getScript` sends an undotted name to `getModule("Base")` and a dotted one to its own prefix, so a non-`Base` recipe answers to **neither** spelling `lookupRecipe` asks, and the row would have FAILED a correct load and skipped the hold (fixed in `45c0915`) |
| 2026-09-10 | 09 | The measured key action is **the last gate flip** (`item.set … lastCookMinute -1`), whose ack is the action moment; `item.update` is demoted to a witness whose `before` block records who actually ran the transition; and the guard read moves **before** the flip | make `item.update` the action and read the guard state after it, as the first probe plan had it | the server's own item tick pre-empts the bus, measured twice: on `td1-20260910-192457` `item.update`'s `before.cooked` was already `true` (the cook block had been entered twice, 1.33–1.67 game minutes apart, so the minute gate had reopened without the flip), and on `td1b-20260910-202029` the transition fired **907 ms before** the flip reached the server. The graded outcome is therefore (c) — the server's own tick ran it — and **causation by the bus is withdrawn, not claimed**. With the guard read left after the flip, the same run would have graded "the tick won" as "`additem` spawned it cooked" |
| 2026-09-10 | 09 | `witness.moddata` is called with **space-separated keys behind an explicit scope** (`player:<user> k1 k2`, `item:<user>/<fullType> k1 k2`, `*` for a census), and pass 1 also ran the **client-side** item census slice 08 deferred | the template driver's comma-joined `KEYS` string, shared with `witness.fields` | the shipped grammar is space-separated, so a comma-joined list arrives as ONE key named `"k1,k2"` and answers a plausible census of nothing — while `witness.fields` genuinely does want a single comma-joined, whitespace-free token (`count == 26` on every reply, both sides, all three snapshots, is what proves nothing was silently dropped). Two grammars sharing one join was the defect; the template is fixed for passes 2/3 |
| 2026-09-10 | 09 | An item-modData census excludes **`customName` *and* `Tooltip`**, and the exclusion set is chosen **per scope** (a player census excludes the four vanilla fitness keys and `hotbar` instead — **but only late in a session**; see the correction at the end of this row) | slice 08's rule, `customName` alone; one exclusion set applied to every scope | `Tooltip` came back on **both** sides of a mod item whose Lua writes no modData at all (0 `getModData` in the tree): `Item.InstanceItem @3505-@3510` passes the script block's `tooltip` to `InventoryItem.setTooltip`, whose **first act** is `getModData():rawset('Tooltip', …)` (C, jar) — so any script `Tooltip =` line is an item modData key on every side that instantiates the item, and its presence is not evidence of a sync. The one-set version reported all four vanilla fitness keys as findings (`td1`'s `census_player.beyondVanilla`, now a *do not cite* row). **Corrected (slice 10, 2026-09-11, `td2-20260910-231655`):** the player-scope set above is a **late-session** reading. The **server's** player modData is *empty at join* — `keyCount 0` at 79.2 / 91.1 / 97.4 s and **4** only from 108.8 s, 22–33 s after `session_ready` — while the client holds 5 (those four plus `hotbar`) throughout. A census taken at join must expect **server 0 / client 5** and must not treat the fitness keys as present on both sides; the item-scope half of this row is unaffected |
| 2026-09-10 | 09 | Cross-side comparison is numeric with a **1e-6 tolerance**, and a graded row says "equal at the bus's resolution", never "bit-equal" | exact equality; the notes' planned float32 fingerprints (server `0.35000002` vs client `0.35`, "one ulp above an exact 0.5" on `getWeight`) | `TK.json` formatted every non-integral number with `string.format("%.6f", v)` **at this pass**, so the bus quantised to six decimals and every ulp-level distinction collapsed before a driver could see it — the tolerance is *exactly* the encoder's own resolution, which is a coincidence worth knowing. Harmless for this pass: every desync it graded is whole-value (0.05 to 1e9), far above the floor. The format widening is deferred (row below) |
| 2026-09-10 | 09 | Slice 07 open question 6 is **closed in halves** — the placement half **M**, the "is the rename required" half **C**: `harness.install` copies a workshop folder to `<mods_dir>/<declared id>` — `LongTermPreservation4220` → `SKITTLE_LongTermPreservation4220` — and `Mods=` carries the id | leave it **C**, as `docs/testing/profiles.md` had it; wait for a later pass | read out of the run's own directory (`td1`'s `folder_check`): `listing ["PZTestKit", "SKITTLE_LongTermPreservation4220"]`, `source_folder_present false`, `mod_info_at_id true`, `ini_mods_line Mods=PZTestKit;SKITTLE_LongTermPreservation4220`, plus the server's own `loading SKITTLE_…` line. The whole-prefix rename answers the behavioural half (is the rename *required*?) that a case-only subject on case-insensitive Windows never could |
| 2026-09-10 | 09 | **Task 1 produces no commit** — its deliverables are the gitignored working notes plus its report, and the first commit of a pass is Task 2's profile; the template's Task 1 Step 6 is corrected to say so | the plan's `git commit -m "Slice 0N: <MOD> static read"` | `.superpowers/` is in `.gitignore`, so that commit is empty by construction, and an empty commit inside a slice's range is worse than no commit |
| 2026-09-10 | 09 | Task 1 treats `data/mod-inventory.json` as **authoritative** for `mod_id` / `layout` / `version_dirs` / `media_at` and cross-checks it against the folder, rather than distrusting it as the plan's cold-start text originally did | follow the stale text: re-derive identity from the folder and treat the row as unreliable | slice 08 replaced the resolver — `mod_inventory.resolve()` imports `mod_lint`'s `version_dirs` / `info_chain` / `read_info`, so the row and the lint cannot disagree by construction. That **delegation** is recorded in the slice-08 `_strip_comments` row above (2026-09-10, `mod_inventory` importing `food_scan`); what is new here is treating the resulting row as authoritative for a teardown's identity read. Recorded because it is the ruling that let Task 1 skip a re-derivation, and because the plan's cold start was corrected by the slice-08 wave. Cost if wrong: one re-read of one row |
| 2026-09-10 | 09 | A commented-out script block is **inactive**: LTP's `/* OBSOLETE */` region holds two whole item definitions, so the static read counts **15** live item blocks / 117 nutrition keys, not 17/135 | count what the file contains; hold every number until a fresh sweep lands | the engine and `food_scan._strip_comments` both strip `/* */`, the definition slice 08's final wave adopted for `tools/mod_inventory.py` (`9f551f6`); and the live count then confirmed it from outside — `items.count` answered `foodByModule {"Base": 722, "Skittles": 14}` at join, which is the 15 live blocks minus `SaltRock` (`base:normal`) |
| 2026-09-10 | 09 | Harness prep before the live run, **1 of 5** (`37e411e`): the parked slice-08 `witness.moddata` scope gate is fixed, spelled as a **lookup table** of five entries rather than as a regex | leave it parked for a dedicated harness slice; write the amendment's `^(player\|item\|global):?$` literally | Lua patterns have no alternation (`\|` is a literal there), so that regex is unwritable; the table is the same set **minus bare `player`**, deliberately, because `player` is consumed as the scope word of a legal call. Slice 08 parked it precisely so a pass with the harness live would close it, and nothing in `exp08-*` exercised the path, so no committed artifact moves |
| 2026-09-10 | 09 | Harness prep **2 of 5**: `item.script <type>` is mirrored onto the **server** (the implementation moves into `shared/` as `TK.scriptValues`; the client keeps a one-line alias and its reply gains `side`) | leave it client-only and infer script symmetry from the files on disk | it is what makes "both sides loaded the same script" a **measurement** instead of an assumption — the two replies came back identical field for field (`HungerChange -60`, `DaysFresh 53`, `IsCookable true`, the four macros absent), which is what attributes every runtime desync in the teardown to the packet rather than to a script mismatch. Duplicating the client's 25 lines was rejected: two copies of a cross-side comparison make the comparison meaningless |
| 2026-09-10 | 09 | Harness prep **3 of 5**: `TK.ITEM_STATE` gains `isCookable`, `actualWeight`, `weight`, `customWeight` — read-only, no setters | leave them and read all four through `witness.fields` only | they appear in `item.get`, `item.state` and in `item.set` / `item.update`'s `before`+`after`, which is what made the pass's sharpest finding readable off a **single ack**: `getActualWeight` 0 on the server against 0.35 on the client, with `isCustomWeight` visible in the same block as the arm that chose it. `getChef` was deliberately **not** added (tempting and jar-confirmed, but outside the amendment); the chef write is verified by a one-getter `witness.fields` call instead, which also keeps `FIELDS` at the notes' 26 so the `count == 26` assertion keeps meaning what it says |
| 2026-09-10 | 09 | Harness prep **4 of 5**: a `chef` **string** setter on `item.set` (the harness's first, refusing an empty value) and a `perk.xp <user> <PerkName>` read | leave both; take the XP discriminator as **C** from the jar | together they turned the notes' C row into **M** — Cooking XP 0 → **2.5** across the transition — an independent confirmation of outcome (c) that does not depend on a console line, plus a **null control** (XP delta 0 with `chef` unset, `td1b`). An empty `chef` write had to be refused because `chef.isEmpty()` reads as "no chef" at `Food.update @759`: it would look like a set and behave like a clear. `perk.xp` is two calls because `getXP(Perk)` lives on the inner `IsoGameCharacter$XP` object, which `witness.fields` (zero-argument getters only) can never reach |
| 2026-09-10 | 09 | Harness prep **5 of 5**: `recipes.craft` gains a **module-scan** fallback (an optional 4th argument to `lookupRecipe`, armed on that command only) | leave it and require every caller to know the module prefix; arm it on `recipes.evolved` too | a non-`Base` recipe answers to neither spelling `lookupRecipe` asks, so the command was silently unable to read any modded recipe (jar: `ScriptBucketCollection.getScript`). Measured working — both spellings missed, the scan walked **977** craft recipes and resolved `Skittles.MakeCuredMeat`, and the reply carries `route: "module-scan"` with `scanned`, so the resolution rule stays visible rather than hidden. Left unarmed on `recipes.evolved` so slice 06's measured readings and reply shape are undisturbed |
| 2026-09-10 | 09 | A **second** harness commit landed mid-pass (`903ccaa`): `lastCookMinute` joins `TK.ITEM_STATE` (read-only) and `item.set`'s ack gains `gameMinute` | fold them into the next pass; keep inferring the gate from `serverWorldAge` as `td1` had to | they are the two counters of the gate at `Food.update @86` (`GameTime.getMinutes() != lastCookMinute`), which `td1` could only reconstruct by arithmetic across three acks. With them the follow-up read the precondition directly and found the transition had already fired with `before.lastCookMinute 23` — the reading that withdrew the causation claim. Both go through the file's existing idioms (`TK.call`; the unguarded `getGameTime()` already at five sites); `td1` predates them and `td1b` is the first artifact to carry either |
| 2026-09-10 | 09 | The follow-up questions were answered in a **new** session writing a **new** artifact (`td1b-20260910-202029`, `38ecf23`) rather than by re-running `td1` | re-run `td1` with the extra reads; append to its artifact; leave the questions open | the standing skew rule — `td1-20260910-192457` is the evidence the teardown's tables cite, it is byte-identical to what the driver at `e3afaa0` wrote, and a re-run would replace measurements with newer ones for no gain. The follow-up asks *different* questions (`getDisplayName` on both sides plus a vanilla control, the second thirst hop, a vanilla modData baseline, the XP null control, the item-tick cadence), so a second artifact is the honest shape; `td1`'s four driver minors (`4239c5d`) are disclosed as script/artifact skew instead |
| 2026-09-10 | 09 | `TK.json`'s `%.6f` float rendering is **not** widened in pass 1; the first harness change of slice 10 does it, disclosed as a rendering change (**it did**: `291f977` shipped `tostring(v)`, the row three below) | fix it here, while the harness was already open; never fix it | changing the number format changes **every** ack the harness emits and the shape of every future artifact — on the day a teardown artifact was being written, and after `td1` had already been measured against the old one. Deferring costs only the ulp-level claims, which this pass does not need (every desync it graded is whole-value). Artifacts written before the change stay as recorded, and the disclosure belongs with the change rather than with the deferral |
| 2026-09-10 | 09 | The cooked-food `thirstChange` halving is a **vanilla** finding whose canonical home is [`modding/patterns.md`](modding/patterns.md) § Measured MP sync facts (the graded server→client row); [`vanilla/eating-pipeline.md`](vanilla/eating-pipeline.md) § MP behaviour gets **one** cross-reference line and no numbers, and the teardown's and FILTER 9's copies stand | move it into `docs/vanilla/` as the Task-3 report's concern 3 asked; delete the duplicates; leave three unowned copies to drift | it is an **item-packet** fact, not an eat-path one, and the packet table is where this library's sync facts live — `eating-pipeline.md` documents `Eat`, which never touches it. The two remaining copies were checked against the canonical row and neither carries a number the row lacks (the teardown adds jar offsets, which is what a deep read is for), so rewriting them would be churn against a live reviewer for no gain in fidelity |
| 2026-09-10 | 09 | The template defects pass 1 hit are fixed **in the plan file** (`docs/superpowers/plans/09-11-teardowns.md`) rather than re-issued as per-pass amendments: `item.script` on both sides, `ItemStatsPacket`'s **43 wire fields of which 39 are item state** (the plan had said 39 flatly; the four extra are addressing and presence flags, so slice 02's count was **not** overturned — the "43 not 39" wording this row first carried was itself wrong and is corrected in the final fix wave), Task 1 Step 6 as a no-op, the `KEYS` grammar, the guard-before-flip ordering, the ≈5 s item tick as a cold-start fact, the harness-iteration rule, `Profile.__repr__`, the stale `doc_lint` 4-findings baseline, and Task 5's push | carry them as amendments for each of passes 2 and 3; leave the plan as written and trust the controller to re-brief | the same file is executed twice more, and an amendment that must be re-issued twice is a defect in the template. The plan is a working document, not a record of what was believed when it was written — these ledger rows are the record |
| 2026-09-10 | 09 | `Profile.__repr__` was added as a one-line change applied directly by the controller (`a16ad09`) rather than through an implementer round | drop the expected string from the plans; open an implementer round for it | both the slice-07 and slice-09 plans quote `<Profile … fixture=… mods=… sandbox=…>` as an offline validation step's expected output, and nothing printed it — the check was unperformable as written. A cosmetic one-liner with a green suite is cheaper applied than dispatched |
| 2026-09-10 | 09 | Task 4's review and Task 5 (the ledgers) ran **in parallel** on disjoint files, with one consolidated fix wave after the pass review | serialize: review → fix → ledgers | the slice-05/06/08 precedent — the reviewer reads Task 4's files read-only while Task 5 writes the two ledgers, the plan, and the three doc ripples Task 4's file list excluded. The one shared risk (an edit to a file under review) was removed by ruling the teardown's and `patterns.md`'s thirst copies untouched (row above). Cost if wrong: a second fix dispatch |
| 2026-09-10 | program | **A slice's implementer tasks do not `git push`, and do not flip a board row to `done`.** The row stays `in progress` with a resume note and the ripples until the pass's whole-branch review has run; the controller flips it and pushes at the close | the 09–11 template's own Task 5 Step 3 (`git push`, row → `done`), which is how slices 01–08 read | a row that says `done` before its review has run is a false statement in the one file a cold-start session trusts first, and a push makes it public. The cost is one controller action per pass; the benefit is that the board never claims more than the evidence. The template is corrected so passes 2 and 3 need not be told again |
| 2026-09-10 | program | **Standing rule (user instruction): iterate on the harness as we go, before a live run, when it makes sense** — jar-confirm every member first, land the additions in their own commit **ahead of** the session commit with `docs/testing/README.md` updated in it, and let the acceptance run be the smoke test. Supersedes the 09–11 template's "do not add commands to the harness in this slice" | freeze the harness per slice and park every need for a dedicated harness slice | the alternative was measured: slice 08 parked a one-line defect and slice 09 inherited it, and each of pass 1's five additions is responsible for a graded row that could not otherwise have been made (the five rows above). The guard against the obvious failure mode already exists in the artifact-skew rule: what a pass may **not** do is change something that reshapes existing evidence (`TK.json`'s float format), and that exception is itself a row |
| 2026-09-10 | 09 | **Task 2 (the profile) proceeded while Task 3 waited** for Task 1's notes round 2 and its re-review, instead of serializing the whole chain behind the notes | hold Task 2 until the notes were final; dispatch Task 3 on the round-1 notes | the round-1 re-review had already cleared the three `[[verify]]` rows Task 2 depends on, while every item still open (the guard-before-flip ordering, the four arms of `Food.getActualWeight`, the float32 tolerance) belonged to the **probe plan**, which is Task 3's input and not Task 2's. Cost if wrong: **one profile edit** — which is exactly what it cost, the module-qualified recipe name (`45c0915`) |
| 2026-09-10 | 09 | **No fourth notes round.** The three plan clauses and two minors the fix-2 re-review raised were implemented **in the driver** (Task 3 amendment 12) and the notes were left standing as the C reference | a fourth notes round, re-reviewed, before dispatching Task 3 | the notes are a gitignored working read, not a deliverable, and a fourth round would have re-reviewed prose in order to make a driver correct. Cost if wrong: **the driver carries the plan delta** rather than the notes — which is where it ended up, disclosed in the Task-3 report |
| 2026-09-10 | 09 | The display-name discriminator's control criterion is **the translation table, not the absence of a `DisplayName =` line**: the six `module Base` foods missing from `media/lua/shared/Translate/EN/ItemName.json` (null `display_name` in `data/food-items.json` — `Base.FruitSaladClay`, `Base.HotDrink{Copper,Gold,Metal,Silver,Tumbler}`) read against `Base.Steak`, which is in it — and **slice 10's session runs those reads, not pass 1** | keep "a vanilla food with no `DisplayName =` line" as the control, as the teardown and the board ripple first had it; run the six reads in pass 1 | the original criterion selects everything and so discriminates nothing: **no** vanilla food carries a `DisplayName =` line (0 of the 1 005 records in `data/food-items.json`, 0 occurrences in `media/scripts/generated/items/food.txt`, 2026-09-10). The translation table does discriminate — those six are exactly the `module Base` foods absent from `ItemName.json`'s 4 889 entries. A **third** hypothesis was found offline at the same time: LTP ships the **B41** `ItemName_EN.txt` layout (`42.20/media/lua/shared/Translate/EN/ItemName_EN.txt:3`) which 42.20.4 may not read at all (`tools/food_scan.py:270-271`), so the mod's name may never load. Three arms now, none settlable without a boot, and pass 1's live work was finished |
| 2026-09-10 | 10 | Pass 2's subject is **`simpleStatus`** (item `2867431511`), taken from slice 08's committed order (`bb418a4`, [nutrition-mods.md](mods-survey/nutrition-mods.md) § The three picks); the profile names the **declared id** `simpleStatus`, not the folder `SimpleStatus` | the template plan's own fallback queue; re-rank the picks at the pass; skip to `AutoCook` | the plan's Step 1 takes the first pick with no teardown yet and slice 08's list is the measured one. **No skip rule fired** (`mod_lint 2867431511` → 0 ERROR, 1 INFO `folder-id`), the mod declares **no `require=`**, no fall-through was used, and **no scoping ruling was needed**: the live `42.16/` tree is 7 client Lua files / 1 580 lines, every one read end to end, plus the `42.14` server file the 42.15 split dropped. The folder/id case difference is degenerate on NTFS, which is why this pass could not answer the second half of slice 07 open question 6 |
| 2026-09-10 | 10 | The `[[verify]]` tier moved **(b) → (a) mid-pass**: Task 2 shipped `verify = []` with the mod's own console print as the took-effect evidence (`a42c8b4`), and Task 3's harness commit added one client row — `text.get IGUI_SS_BARTITLE_HAPPY` expecting `"Happiness"` (`291f977`) — keeping the console grep beside it | stay at tier (b) for the whole pass, as Task 1 concluded; reach the mod's `SimpleStatus.VERSION` global instead | tier (b) was **correct on the shipped bus at `a42c8b4`** — the mod ships no scripts and its only persistent state (`SimpleStatusConfig`) does not exist until a human moves a bar — so the ruling changed because the **bus** changed, not because the reading did. The probe cannot pass on a miss: `getText` returns the key itself when a key is absent, vanilla declares no `IGUI_SS_` key, the mod declares `IGUI_SS_BARTITLE_HAPPY = "Happiness"` (`IG_UI.json:7`), and `"Happiness"` is not a substring of the key. Measured `ok=True` on `run-20260910-230752`. The same commit also carried the profile's comment fixes (the inverted failure signature; "by construction" → "on the shipped bus as of `a42c8b4`"), because it was already editing that file |
| 2026-09-10 | 10 | Cross-side readings are graded against a **signed, per-snapshot band** built from the driver's own wall brackets — `δ_min = t_srv_before − t_cli_after`, `δ_max = t_srv_after − t_cli_before`, macros in `[r·δ_min, r·(δ_max + 1 s)]` — with the **client read first** at every tag, weight carrying the **opposite** sign plus a one-float32-ulp floor (7.63e-6 kg at 80 kg), and any snapshot inside a write's push window reported as a **latency reading, not a band test** | pass 1's unsigned tolerance with its "the client holds the larger value; a negative gap falsifies the skip" falsifier; one band reused across snapshots; a point band at a single δ | the server's copy is a **ramp** and the client's a 1 Hz **staircase**, read ~1 s apart on two bus round trips, so the sign of `client − server` is a property of the **driver's read order**, not of the game: a server-first loop puts every macro band entirely negative (≈ −0.26 kcal at 0.256 kcal/s) and pass 1's wording would have graded a perfectly healthy mirror as falsifying `Nutrition.update @42 L75` — the headline mechanism of the teardown. Weight is opposite-signed because the client never recomputes it (`updateWeight`'s `GameClient.client` skip `@317-@320 L198`) and narrows through `save`'s `d2f` / `load`'s `f2d`, so its band is `[−ρ·(δ_max + 1 s) − 7.63e-6, −ρ·δ_min + 7.63e-6]` with the arm **read from the flags**, not guessed. Cost: the driver must bracket each side's read itself, which pass 1's did not. Result: all 30 macro readings over six snapshots landed inside their own band (`td2-20260910-231655`) — consistent with a 1 Hz push, never stressed. **Read the count with its grading**: only **four** of the six snapshots are band-**graded**; the other two (`t0`, `t2+0s`) sit inside a write's push window and are latency readings by the settle rule, reported and ungraded, so the graded population is 20 readings and the other 10 are a bonus |
| 2026-09-10 | 10 | Harness prep before the live run, **1 of 4** (`291f977`): `TK.json`'s non-integral branch becomes **`tostring(v)`**, not one of the `%g` candidates the amendment named, and `±Inf` renders `null` | `%.9g` / `%.10g` / `%.17g`, measured against each other first; leave `%.6f` for another pass | Kahlua implements its **own** `string.format` (`StringLib.appendSignificantNumber` + `roundToSignificantNumbers`, a decimal re-multiplication), so no C `%g` guarantee applies and `%.17f` overflows `Math.round` above ~92; the three could not be measured offline (the jar's only `main` is a Kahlua demo thread, and this machine has a JRE — no `javac`) and measuring them live would put an unproven conversion character inside the encoder **every** ack passes through, whose failure mode is a Java exception mid-session. `tostring` needs no measurement: `KahluaUtil.numberToString` is `Double.toString` for a non-integral double — a language-guaranteed round trip — and `String.valueOf((long) d)` for an integral one, so integers stay integers and no key changed name or type. Measured on the bus anyway: **1 225 JSON number tokens (871 non-integral, 354 integer), 0 non-round-tripping** — the **1 292** first recorded has no recorded provenance and does not reproduce as any census of the file (`\d+` gives 2 339) — cite 1 225. What it bought: the client's weights are exactly float32 (6/6) and the server's are not (5/5), so `Nutrition.save`'s `d2f` narrowing is **visible on the wire**, C → M. `±Inf` previously produced an *unparseable* ack (the whole reply lost), so `null` is a strict improvement — though that claim is **C**, never exercised |
| 2026-09-10 | 10 | Harness prep **2 of 4**: `TK.nutritionSnapshot` gains `incWeight` / `incWeightLot` / `decWeight`, read through `TK.call` and emitted **only when the member exists** | infer the arm from the macros; read the three through `witness.fields`; call them directly like the five macros beside them | `false` is a real reading, so an absent member and a false flag must not collapse into the same JSON — hence the optional-read idiom here, while the five macro getters stay direct calls on purpose. They appear on both sides in every `nutrition.get` and `stats.get`, and they made the pass's sharpest claim **M**: the client's flags agreed with the server's at all six snapshots **and** with the arm recomputed from that snapshot's own macros, so a client **does** run `updateWeight`'s flag half and stops exactly where the jar says it stops, at `setWeight`. They also fix the weight band's sign, which would otherwise have been a guess. One correction fell out of the same dump: `setIncWeightLot(true)` is set on the ×2 arm as well as the ×3 arm (`@194-@226 L179-L181`), so the flag starts at carbs **or** lipids **> 400**, not 700 — only the 700 arm was exercised live |
| 2026-09-10 | 10 | Harness prep **3 of 4**: a **server-side `moddata.set <user> <key> <value>`**, the exact twin of the client's, writing with a plain assignment rather than `rawset` | leave it client-only and take the transmit's wipe from the jar as C; write with `rawset` as the amendment specified | without a server-side write the transmit experiment has nothing to lose: the vanilla difference set is **empty in the only direction that matters** (the client's five keys are a superset of the server's), so a planted server key is exactly what turns `KahluaTableImpl.load @5-@6 L333`'s wipe-before-rawset from C into **M** — and it did, `pzt_ss_server` vanishing from the server's census the moment the client transmitted while `pzt_ss_client` and `hotbar` arrived, the receiving table becoming *exactly* the sender's. Plain assignment because a `KahluaTableImpl` carries no metatable (the two are identical here), the client's shipped `moddata.set` has used it since slice 01 and is measured, and `rawset` appears **nowhere** in the game's own `media/lua` — an absent global is Kahlua's uncatchable "tried to call nil", this time on the **server** |
| 2026-09-10 | 10 | Harness prep **4 of 4**: a client **`text.get <key>`** → `{key, text, miss, side}`, nil-checked and called **directly** rather than through `TK.call` | route it through `TK.call` like every Java member; stay at tier (b) and grep the console only | `getText` is a Lua **global**, not a member on an object, so `TK.call` / `TK.field` — which index a subject — cannot reach it; the harness's own idiom for globals is a nil check plus a direct call (`getSandboxOptions`, `getEvolvedRecipes`), and reading an undefined global in Lua is `nil`, never a raise. A **miss returns the key itself**, reported as `miss: true`, which is what keeps the probe honest. It is the whole reason this pass has a tier-(a) row (the tier row above), and it generalises: any mod with no scripts but its own prefixed translation keys now has a load probe. Slice 11 adopted it the same day on `UI_AutoCookMode` |
| 2026-09-10 | 10 | Pass 1's **display-name control was run inside this pass's session** and is answered **inside vanilla**: an item absent from `ItemName.json` reads `getDisplayName() == getFullType()` and `getActualWeightUnmodded() == 0` on **both** sides (`Base.FruitSaladClay` against the control `Base.Steak`, `td2-20260910-231655`) | spend a dedicated session on it; leave pass 1's open question standing with three unseparated hypotheses | the reads were a ripple with no owner and cost two RCON spawns inside a session that was booting anyway. Hypothesis (i) *no translation entry* is **sufficient on its own**, so (iii) *mod translations are not consulted on a dedicated server* is **not required** to explain pass 1's `Skittles.CuredPork` symptom, and both sides agreeing rules out a sync explanation. **(ii) stays open by design** — separating "42.20.4 never loads a B41-layout `ItemName_EN.txt`" needs a write into the read-only mod tree. n = 1. The result was written into the pass-1 teardown as a dated addition; no pass-1 sentence was rewritten |
| 2026-09-10 | 10 | Pass 1's **client-copy control REOPENS its freeze row** rather than confirming it, and the reconciliation is recorded as a **hypothesis**, not a mechanism: `patterns.md`'s row is marked **CONTESTED** with both readings quoted | leave pass 1's "the client's copy does not tick" standing; assert the `sendItemStats` mechanism as the answer; drop the pass-1 row as wrong | measured: after a server `item.set admin Base.Steak heat 2.0`, the client's `getHeat` / `getCookingTime` moved (2 → 1.697029948234558, 0 → 0.1182333305478096) over 10.5 s and were **bit-identical** to the server across a 0.5 s read offset during which an independently ticking copy would have decayed further. "Push-updated" is the parsimonious reading; "both sides tick and converge on the same float32" is not excluded by this session, and the reconciling candidate — the per-game-minute `sendItemStats` inside `Food.update`'s cooking branch, gated at `heat > 1.6f`, which would explain a push in pass 2's window and none in pass 1's — is **C and unresolved**. The separating probe (pin `heat` at **1.2**, two reads 10 s apart on both sides) is named in all three docs that carry the claim and is carried by slice 11. Asserting the mechanism would have made three docs move together on an inference |
| 2026-09-11 | 10 | The template defects pass 2 hit are fixed **in the plan file** (`docs/superpowers/plans/09-11-teardowns.md`): the worked example's `FIELDS`, the cadence line's "90 game-minutes/day", the cold-start command list (slice 10's client `text.get` and server `moddata.set`, slice 11's shared `lua.global`), the cold-start claim that a client's copy of a server-spawned item does not tick (marked **CONTESTED** by this row's own pass; pass 3 then rewrote the same paragraph as **resolved per arm**, which is how it reads at HEAD), and the worked example's three factual disagreements. **What this row did NOT close, and the final fix wave did:** carried items A5 (the `profiles.md` install-path clause), A6 (slice 07 open question 6 as two halves wherever it is quoted), A10 (`testing/profiles/README.md` still calling `teardown-simplestatus.toml`'s `verify` "empty by ruling") and A11 (four docs describing `%.6f` in the present tense) were **left to the wave** by name, and the wave closes all four | re-issue them as pass-3 amendments; leave the plan as written and let the controller re-brief | the same rule as pass 1's row: the file is executed again and an amendment that must be re-issued is a defect in the template. `FIELDS` was the worked example's only **wrong** value and load-bearing — the five macro getters live on `Nutrition` while `witness.fields`'s player subject resolves to the `IsoPlayer`, so every name would have landed in `missing` (slice 08 measured exactly that); it now names the five `IsoPlayer` getters pass 2 used and says where the macros actually come from. `DayLength = 4` is **90 real minutes per game day** — the number and the cadence formula were right and only the label was wrong, but the label is what a later pass would reason from. The rest is pass 2's own evidence written back into the template rather than left to contradict it |
| 2026-09-11 | 10 | A table cell that would need a **pipe character** is rewritten to avoid one, in every table `doc_lint` grades; the tool itself is **left as it is**, with the trap written into `tools/README.md`'s `doc_lint` entry | teach `_tables()` to honour a backslash-escaped pipe (a `tools/` edit plus its test); leave the trap undocumented and let the next author rediscover it | `doc_lint._tables()` builds a row's cells by stripping and splitting on the pipe character with **no escape handling**, so a backslash-escaped pipe *still* splits the row and shifts every later cell one place — which moves the `Ev` column and fails a correctly graded row, or, when the shift lands on another graded cell, grades the wrong one. Task 4 hit it on a real row (three `SS_tog_*` key names written as one alternation) and rewrote the cell to name the three keys in prose, which reads better anyway. The splitter is deliberately simple, a fix touches `tools/` plus a test — a wave's edit, not a ledgers task's — and nothing in the corpus needs it today. This ledger's own five-cell rule is unchanged: a pipe inside a cell here is escaped and counted as escaped |
| 2026-09-11 | 10 | The carried item "`IncWeightLot` at > 400 wherever 700 is quoted" is **recorded as checked with no surviving target**, rather than hunted further | keep it open on the fix list; add a 400 sentence to a `docs/vanilla` page for symmetry | the two 700s in the library are both correct as written: [`vanilla/nutrition-core.md`](vanilla/nutrition-core.md) already carries **both** arms (`> 700` ×3 and `> 400` ×2), and [`vanilla/body-stats.md`](vanilla/body-stats.md)'s `gainThreshold = 700` is the **WEIGHT_GAIN base**, a different 700 entirely. The correction is only about *which arm sets the flag* — both — and no `docs/vanilla` page states that at all, so it lives where it is observable: `patterns.md`'s `updateWeight` row and the slice-10 teardown. Also checked: the pass-1 teardown has **no host sentence** for it — no `700`, no `IncWeightLot` and no weight-flag content anywhere in it; its single `fitness` mention (`:369`) is the modData-census line, a different subject, already corrected in place by slice 10's Task 4 |
| 2026-09-11 | program | **The three passes of the 09–11 template overlap.** A later pass's static-read task (and, once its profile exists, its harness prep) runs while an earlier pass is still in review, on disjoint files, with **one live session at a time** and one implementer per file | serialize the passes end to end; run them fully in parallel | the tasks that collide are the *live* ones (one game client, one set of ports) and the *shared* files (the harness Lua, the plan, the two ledgers); a static read touches neither — pass 2's Task 1 ran during pass 1's close and pass 3's Task 1 and profile ran during pass 2's session and doc. It cost one re-read of a plan section that the pass-1 wave edited underneath pass 2's reader, which is the predicted cost. The rule it needs: a pass never stages another pass's paths, and a ledgers task commits **by path** because another implementer's work may be in the index |
| 2026-09-11 | 10 | The text-only fixes from Task 3's review were applied **directly by the controller** as a residuals commit (`0449fa3`) rather than through an implementer round | a fix round with its own dispatch and re-review; fold them into Task 4's commit | they were four wording corrections to already-committed prose (a false jar claim in a harness comment, a profile comment splice, a README inventory sentence, and the unmeasured `±Inf` claim re-graded **C**) with no measurement and no Lua behaviour behind them, on files Task 4 was not editing. The same precedent as pass 1's `Profile.__repr__` row: a text-only correction with a green suite is cheaper applied than dispatched, provided it lands in its own commit so the range stays readable |
| 2026-09-11 | 11 | Pass 3's subject is **`AutoCook`** (item `3388721641`), the third and last of slice 08's committed order (`bb418a4`, [nutrition-mods.md](mods-survey/nutrition-mods.md) § The three picks); the profile names the declared id `AutoCook`, which here **equals** the folder name. With it the **slice-08 queue is exhausted**, and the fall-through list is **re-labelled the future-picks list** in the catalog rather than left standing or deleted | re-rank the picks at the pass; take a fall-through early (MoodleFramework was the standing candidate); keep the list labelled *fall-through* with an empty queue above it; delete it | the plan's Step 1 takes the first pick with no teardown yet and slice 08's list is the measured one. **No skip rule fired** (`mod_lint 3388721641` → 0 ERROR, 1 WARN `mod-info-place`), the mod declares **no `require=`** (nine `mod.info` keys, and no `incompatible=` / `loadModAfter=` / `loadModBefore=` either), **no fall-through was used in any of the three passes**, and **no scoping ruling was needed**: 10 Lua files / 2 155 lines were read end to end, the three files that exist in both trees read in the `42.13/` copy with the `common/` copy covered by an exhaustive `diff`. A list labelled *fall-through* with nothing left to fall through from would be a false statement, so it becomes the future-picks list in the same order — and two of the three now carry a pass-3 reason of their own: MoodleFramework is **whole on 42.20.4** under the merge rule this pass measured (its `common/`-only tree is loaded, not ignored), and `SomewhatTraitsCore` is still the corpus's only **player** macro write. `CleanUI` stays dropped as past a 2 h teardown |
| 2026-09-11 | 11 | The **`common/`-vs-version-directory merge rule is measured**: the version directory's file **wins** a same-relative-path collision, `common/` supplies everything the version directory does not ship, and both end up in **one** Lua state — graded **outcome M** (n = 1 session, `td3-20260911-001948`) and **mechanism C** (the jar), with the statement **bounded** to a mod whose `getVersionDir()` resolves to a tree that ships colliding files. It closes catalog open questions **1** (whole) and **3** (derived, graded **C**) | leave OQ 1 open and cite the wiki (**W**); grade the whole claim **M**; state the rule unbounded, which is how it will be quoted | **five readings agree and none contradicts.** M1, the loader's own five `overrides` lines — two translation JSON over vanilla, three client Lua over `common/`, **no `.png` tail**, which is what pins `getVersionDir()` to `42.13/`. M2, the load-time modData key — `common/` ran and the chain completed, **neither direction** (row below). M3, the mod's two translation files resolving beside two intact vanilla controls — consistent, **non-discriminating**, and it measures the separate fact that `Translator` **merges** rather than reading through `activeFileMap`. M4, `lua.global` finding both 42.13-only discriminators (`AutoCook.acceptIngredient`, `baseAcceptsSpice`) live on the client while `ISContinue` rules out the inert branch. And M4b, unplanned: `_G.AutoCook` has **`keyCount 49`** = 23 file-scope scalars + 15 + 4 + 7 functions, where a common-wins state gives **47** — an arithmetic fifth reading. The bound is the honest part: a version dir that ships no colliding file cannot exhibit the rule at all. The independent negative is **zero** `HasTrait` / `getTypeString` raises — both members were removed in 42.20.4 and only the `common/` copies call them — itself bounded to `common/AutoCook.lua:39`, which is load-bearing because `:38` demonstrably executed on this character, widened by the Task 4 review (8906b79) to the whole window-build path (`common/AutoCook.lua:39`, `AutoCook_AutoCraftRecipes.lua:39`, `ISCharacterCook.lua:221` via `createChildren:23`) and explicitly **not** to `common/ISCharacterCook.lua:50`, the one `prerender` site |
| 2026-09-11 | 11 | Harness prep for this pass is **one addition** (`d8cc34e`, H4): a **shared** `lua.global <name>[.<field>…]` walking `_G` by dotted path, answering `side` / `name` / `resolved` / `type` / `value` / `keyCount` / `failedAt`, **read-only and never calling** a function it finds (the type *is* the reading), shipped **singular** — one name per call — rather than the notes' multi-name block | infer the winner from M1 and M2 alone; a multi-name block command as the notes specified; reach into the mod through `TK.call`; park it for a harness slice | no shipped command could separate version-wins from common-wins: M1 reads the loader's **map** and M2 reads a key **both** copies write at the same line, so the discriminator has to be the resulting **Lua state**. It is the first harness addition of the wave that needed **no jar confirmation**, because it touches no Java at all — `_G` is Kahlua's own table and the walk uses `type` / `pairs` / `string.gmatch` / `tostring` — so the idiom was confirmed against the game's own Lua instead (`ISXuiBuilder.lua:10-35` splits a dotted name, starts at `_G` and guards every hop with `type(container[v]) == "table"`, which is the shape shipped). Singular cost 22 bus calls instead of 2 (≈ 19 s of a 179 s session) and lost nothing: every name carries its own `resolved` / `failedAt`. Presence is tested with `v == nil`, never truthiness, and that is not academic — `AutoCook.Verbose` read `false` and was correctly reported **present**. `TK.version` was added as a control name the notes did not list: it is the only thing separating "the `_G` walk is broken on this side" from "this global is absent on this side", and the server's eleven absences are readable only because of it |
| 2026-09-11 | 11 | **The load-time modData key proves `common/` ran and the require chain completed — not that "both trees ran".** The `[[verify]]` row 2 comment, the notes and the Task-3 amendments all carried the stronger claim; it was corrected in the harness-prep commit **before** the session, and the row was kept as a **gate** rather than demoted to a timed driver read | keep the pre-session wording; drop M2 as non-evidence once M4 existed; demote the row to a non-blocking driver read against the timing risk | the `common/` copy of `AutoCook.lua` writes the same key at the same line (`:38`), one line **before** its B41-era `HasTrait` call at `:39`, so a green row is consistent with **either** copy having won — only M4 separates them, and a profile comment asserting otherwise would have been the pass's own evidence trail stating a thing the pass then disproved. What M2 *does* prove is load-bearing and nothing else in the pass proves it: `player:getModData().AutoCook` exists **at join with no user input**, so `ISCharacterCook:createChildren` ran, so `addCharacterPageTab` ran — and that function is defined **only** in `common/…/ISCharacterInfoWindow_AddTab.lua:2`. Keeping it a gate was justified by three archived consoles putting `createPlayerData` at frame 0 against `client_ready` at ~f:19, and it read green twice live (`session_ready + 0.5 s` on the acceptance run, `+ 1.0 s` on the session), so the documented fallback never fired |
| 2026-09-11 | 11 | The AutoCook/CleanUI collision is recorded **split four ways** and the two-mod, two-order profile is **not run — it dissolves**: the `ISCharacterInfoWindow` half is closed cold, the **load-order** half dissolves, the **version** half is bounded (CleanUI ships `ISInventoryPaneContextMenu.lua` in **3 of its 7 trees** — `42.15`, `42.16`, `42.19`; the other four, `42.12`–`42.14` **and `common`**, do not have the file), and the **runtime** half stays **open**, booked for slice 12/14 | run a two-mod, two-order profile in this pass; assert the conflict from the static read; drop the collision as speculative | `activeFileMap.put` is unconditional and `LuaManager.LoadDirBase` adds **vanilla's block first** (`PZArrayUtil.addAll @371-@373`, `HashSet` dedupe `L1208-L1222`), so a mod file sitting at a **vanilla** relative path runs in vanilla's slot and CleanUI's fork of `ISInventoryPaneContextMenu.lua` wins in **both** orders — the experiment's two arms are the same arm, and running it would have spent a live session to measure a tautology. The half that is real is a **runtime** question — CleanUI wraps the `OnPreFillInventoryObjectContextMenu` trigger in `pcall` and a printed failure line, and that trigger is AutoCook's **only** entry point — and nothing on the shipped bus can right-click, so it needs a `triggerEvent` route the harness does not have. The `ISCharacterInfoWindow` half needed no run either: CleanUI ships no such file in any of its seven trees and the string `addCharacterPageTab` is absent from the whole item. The version census is why the claim has to be bounded at all — on a build resolving to `42.13/` or `42.14/` the collision does not arise |
| 2026-09-11 | 11 | The carried pass-1/pass-2 contradiction was **settled inside this pass's session**, and `patterns.md`'s **CONTESTED** client-copy row becomes **resolved per arm**: the per-game-minute `sendItemStats` inside `Food.update`'s **cooking branch** is the carrier — **M per arm** (three arms, three sessions, n = 1 each) and **mechanism C** | a dedicated follow-up session; leave the row CONTESTED with both readings quoted; grade the mechanism **M** on the strength of the two agreeing arms | the probe cost one RCON spawn, one `item.set` and four `witness.fields` reads inside a session that was booting anyway — the same economy as pass 2's display-name control. Pinned **below** the 1.6 cooking gate (`heat 1.2`), the client's copy was **bit-frozen** — `1.2000000476837158` at both reads, **12.54 s** apart — while the server's decayed to the 1.0 floor; pass 2 pinned **2.0**, above the gate, and its client copy moved bit-identically to the server's. Two controls inside the same probe keep it honest: the pin's own `sendItemStats` **did** arrive (both sides read the identical float at read 1, so the freeze is not "the client never saw the value"), and the item-scope census reproduced slice 08's `customName` finding on the same instance. The refinement it forces is written everywhere the word **RESOLVED** now appears: below the gate the client's copy does not tick **at all**, which **narrows** pass 1's "can tick" clause rather than proving it, and the bounds travel with it — one item, one 12.54 s window, one fixture, with frozen/non-cookable items and every other push path untested. Three documents would need the same dated correction if a later pass finds a fourth arm |
| 2026-09-11 | 11 | The `data/mod-inventory.json` **scope gap** is recorded as a **ripple plus three prose statements**, not as a data or tool change: `signals` / `stats` / `top_events` describe the **live version folder only**, which for AutoCook hides **7 of 10 Lua files, 1 238 of 2 155 lines, the mod's only event registration, its only vanilla patch and its whole translation set**. `media_at` stays the guard on a zero; one now-stale clause in `tools/README.md` is corrected with it and `data/README.md` is left to the ripple | widen the scan to `common/media` (a `tools/` change plus tests plus a regenerated dataset); edit `data/README.md` here; say nothing and let the teardown carry it alone | the numbers are what make the existing guard concrete — `top_events: []` for a mod whose **entire** entry point is an event registration is exactly the failure `media_at` exists to warn about, and 177 of the 230 rows list a `common/media` this scan never reads. Widening it is slice-sized, not ledger-sized: it moves those 177 rows, invalidates dated counts across the catalog, and requires the merge rule **in code** — which this pass measured but deliberately **bounded**, so encoding it would over-apply it. The statements therefore live where a reader meets the hazard (the teardown's § Architecture, `patterns.md` KEEP 10, and `tools/mod_lint.py`'s `media_root` docstring). The `tools/README.md` clause is the one thing this pass genuinely **disproved**: its `mod_inventory` entry read "`common/media` … is not counted here and **no merge rule is asserted**", which is no longer true of the library, so it now names the measured rule and its bound while keeping `media_at` as the guard on a zero |
| 2026-09-11 | 11 | `mod_lint`'s `mod-info-place` WARN text and [`testing/profiles.md`](testing/profiles.md) § Open questions 1 are **left as they are**, and the jar precision this pass re-dumped is recorded **here** instead: `ZomboidFileSystem.searchForModInfo(File, String, ArrayList)` returns the first `mod.info` **whose id matches the requested id**, in `File.list()` order — **not** simply the first `mod.info` found — and every `mod.info` it passes on the way is still registered into `modIdToDir` (`L727`) and appended to the caller's list (`L728`) | rewrite the WARN text now; close open question 1 on this reading; leave the precision unrecorded until slice 12 re-dumps it | this is a **third** model of `mod.info` resolution beside `mod_lint.info_chain` / `pzt.mods.mod_id_of` (newest version dir → `common/` → root) and `getModVersionDirName`, and the three have never been reconciled against a **running** build — which is what open question 1 is for and what slice 12's mod-anatomy doc is scheduled to settle from the engine. This pass's subject **cannot discriminate**: AutoCook ships exactly **one** `mod.info`, in `common/`, so every model answers the same id, and the WARN it fires is about **placement**, not resolution. Trading a documented WARN for an unmeasured one on a bounded jar read the pass could not exercise is the wrong direction. The usable half is negative and belongs on the record either way: any wording anywhere that says "the first `mod.info` found wins" is **wrong in its first half** and right only about the side effects |
| 2026-09-11 | 11 | The template defects pass 3 hit are fixed **in the plan file** (`docs/superpowers/plans/09-11-teardowns.md`): the cold-start `common/` sentence gains the **measured merge rule with its bound**; the cold-start **CONTESTED** client-copy paragraph becomes **resolved per arm** with the probe's own numbers; and the picks decision point records that the slice-08 queue is now **exhausted** and where the future picks live. The bus list already carried `lua.global` (pass 2's ledgers commit added it as `d8cc34e` shipped) and was **verified, not re-edited** | leave the plan alone, since the template has now run its three passes and is not scheduled again; re-issue the fixes as amendments to a fourth run | the same rule as passes 1 and 2: a file that will be read again must not contradict its own passes' measurements — and it **will** be read again, both by slices 12–14 (which copy its structure and inherit its cold-start facts) and by any future pick off the catalog's new future-picks list. The **CONTESTED** paragraph is the load-bearing one: it instructed a probe designer that a client's copy of a server-spawned item might or might not move and to assume neither answer, and it named the separating probe as still-to-run. That probe **has** run, so left standing the paragraph would send a fourth pass to repeat it. This is the **third** consecutive pass to add corrections inline, and pass 2's concern stands unrelieved: the file now wants a short `## Corrections applied` section rather than more parentheticals, which is a wave's edit and not a ledgers task's |
| 2026-09-11 | 10 | **Never `git commit --amend` on this branch while another implementer is live, and commit with PATHSPECS only** — the durable ledger now carries the rule the SDD ledger already stated | keep the rule in the SDD ledger only; add a hook or a wrapper; say nothing and rely on the incident being remembered | an implementer amended slice 11's already-committed **live session** commit (`3f4b646` → `05a40d3`) while two read-only reviewers were reading the tree at it and a second implementer was working. The amend was caught by the same-command detection, and the recovery was `git reset --hard 3f4b646` — verified after the fact: **`3f4b646` is intact and reachable from HEAD, `05a40d3` is dangling and reachable from nothing**, so no work was lost and no reviewer's citation was invalidated. What made it recoverable was luck of timing, not the tooling: an amend rewrites a commit other agents have already cited by hash, and a bare `git commit -a` sweeps another implementer's in-flight files into it. Both failure modes are removed by the same two habits, which is why one row states both |
| 2026-09-11 | 10 | `mod_lint`'s `mod-info-place` WARN text **is** reworded (it now names this lint's own model and points at `profiles.md` open question 1) and the bounded jar read **is** written into that open question — **superseding the "left as they are" half** of slice 11's row on the same subject, which stands for everything else it records | leave both as slice 11 ruled; reword the WARN but leave `profiles.md` alone; wait for slice 12 | the two rulings answer different questions. Slice 11 declined to **trade a documented WARN for an unmeasured one** on a jar read its own subject could not exercise, and that reasoning holds. What the wave removes is narrower and is the half slice 11's row itself calls wrong: the WARN **asserted an engine read order** ("B42 reads the version folder first") that `ZomboidFileSystem.getAllModFoldersAux` contradicts by testing `<mod>/common/mod.info` first. The replacement asserts nothing about the engine — it names the lint's model and points at the open question — so no unmeasured claim is introduced and open question 1 stays **open**, with the two call sites recorded under it and slice 12 still the thing that settles it. The precision slice 11 insisted on travels with the text: `searchForModInfo` returns the first `mod.info` whose **id matches**, in `File.list()` order, not simply the first found. `tools/tests/test_mod_lint.py` holds the new text to naming a model, and `docs/superpowers/plans/09-11-teardowns.md`'s quoted expected output moved in the same edit |
