# Progress board — research slices

Status of every slice in the [research-slices spec](superpowers/specs/2026-09-09-research-slices-design.md).
A session takes the first `ready` slice whose dependencies are `done`; see the
spec's run loop. Statuses: `ready` · `in progress` · `done` · `blocked`.

| # | Slice | Status | Date | Commit | Outcome |
|---|---|---|---|---|---|
| 01 | P1a · Intake pipeline | done | 2026-09-10 | cd0f936..419f16a | `docs/vanilla/eating-pipeline.md` (67 modifiers, 24 measured; server owns Nutrition in MP); tools wiki_mirror/doc_lint, `pzt doctor`, harness experiment commands, 7 mirrors, tracked artifacts; acceptance in the plan file |
| 02 | P1c · Food item model & lifecycle | done | 2026-09-10 | 0c3d6b7..e44610c | `docs/vanilla/food-item-model.md` (114 keys, aging/cooking/evolved models, 36 measured rows); lifecycle harness commands + `s02_lifecycle.py`; artifact `exp02-20260910-030433`; 3 mirrors; ItemStats packet analysis incl. the zero-field leak |
| 03 | P1b · Body side | done | 2026-09-10 | 22ce6c2..5b9b959 (interleaved with slice 04) | `docs/vanilla/body-stats.md` (passive burn, hunger/thirst rates and multipliers, moodle thresholds, weight bands, appetite traits; 57 measured rows, `exp03-20260910-045523`); body-side harness commands (server `stats.get`, `trait.set`, `player.sleep`, ...; client `player.walk`) + `s03_body.py`; 5 mirrors; corrections across nutrition-core/eating-pipeline/patterns (client computes and discards weight; recovery-mod deficit penalties are dead code; FOOD_EATEN timer is game-time; THIRST level-4 health loss at 5x the hunger rate, found through the slice-04 death run) |
| 04 | T2 · Accelerated nutrition scenario | done | 2026-09-10 | 3b1ac80..afac230 (interleaved with slice 03) | harness test layer (`TK.test`, `t:at/every/eventually`, `test.run` on the server bus) + `pzt scenario --side server`; three 3-day runs at 30x (`scenario-20260910-05{2624,4012,5029}`): weight model verified to sub-gram accuracy (+2.526 vs +2.551 kg gain, -1.426 vs -1.412 kg fast); run 1 died of thirst at game-hour 35 (`setCalories` is not food; THIRST level-4 health drain found and confirmed on the jar); `nutrition-core.md` § Verified on server; `pzt doctor` port states; scenario verdicts now fail on server/client errors |
| 05 | P4a · Food scanner + dataset | done | 2026-09-10 | 31478dd..1c4dfa5 | `tools/food_scan.py` (shared nesting-aware script parser + dataset builder), `data/food-items.{json,csv}` (722 foods, 150 drainables, 133 fluid containers, 61 fluids; 61 curated CSV columns; per-litre fluid nutrition with per-container columns), `docs/vanilla/food-dataset-notes.md`; harness `items.count` / `fluid.script` / `drink`; live count cross-check + ten-item spot check (`exp05-20260910-084109`, 10/10) and a drink probe (`exp05b-20260910-093307`: 0.3 L Cola = +120.0 kcal, per-litre confirmed); findings: vanilla `HotDrinkRed` → undefined `Base.MugRed`, enum-backed fluids answer an empty `getFluidTypeString()` |
| 06 | P4b · Recipes & cooking dataset | done | 2026-09-10 | ab26d41..405ea79 (interleaved with slice 07; the wave's artifacts-README edits landed in 10581d0) | `tools/recipe_scan.py` (imports slice 05's parser), `data/recipes.{json,csv}` (969 craftRecipe blocks, 0 legacy; nutrition deltas under the loader's use semantics — an unflagged food input amount is hunger units, measured live: `exp06b-20260910-120123`), `data/evolved-recipes.{json,csv}` (63 recipes, 6 881 ingredient rows incl. the two drainables the plan missed; hunger clamp and dried-food thirst skip applied), `replacements` links; harness `recipes.count/.evolved/.craft` + `item.use`; live cross-check `exp06-20260910-112726` (969/63/0; five ingredient lists name-for-name); `docs/vanilla/recipes-dataset-notes.md` |
| 07 | T1 · Profile builder | done | 2026-09-10 | aa61080..73c8416 | `testing/pzt/profile.py` + `testing/profiles/*.toml` (mods under test by workshop id or path, sandbox overrides merged into the fixture's SandboxVars, `[[verify]]` bus probes), `pzt run/scenario --profile` with a fail-fast on a missing mod; `tools/mod_lint.py` (L0 layout lint, nine rules; corpus 230 mods: 84 findings, 3 ERROR / 30 WARN / 51 INFO at 2026-09-10 13:47, counts dated); live acceptance with KeenPerception `3685392864`; `docs/testing/profiles.md` |
| 08 | P3a · Nutrition-mod catalog | done | 2026-09-10 | 726d31a..c50fb30 | `docs/mods-survey/nutrition-mods.md` (installed corpus 230 mods / 179 items with engine-resolved ids, script-nutrition signal, `media_at` guard; public Workshop sweep 180 ids, 3 installed, none nutrition-relevant, fetched 2026-09-10 16:26); picks SKITTLE_LongTermPreservation4220 (09) -> simpleStatus (10) -> AutoCook (11); harness `witness.fields`/`witness.moddata` + live probe `exp08-20260910-152944` (item modData `customName` is a deserialization side effect; rules for 09-11); `tools/mod_inventory.py`, `tools/workshop_search.py`, `data/mod-inventory.*`, `data/workshop-search.*` |
| 09 | P3b · Teardown 1 | done | 2026-09-10 | d3715c5..564afb8 | SKITTLE_LongTermPreservation4220 (3774789651) torn down: docs/mods-survey/teardowns/longtermpreservation4220.md (23-row measured MP table); two live sessions td1-20260910-192457 + td1b-20260910-202029 (the server item tick ~5 s runs OnCooked on a minute-gate rollover; ItemStatsPacket 43 fields / 39 item state — actualWeight carried, isCookable/offAge/offAgeMax/lastCookMinute/customWeight not; a cooked food thirst halves once per server->client hop, vanilla; getActualWeightUnmodded 0 when displayName == fullType on both sides; XP discriminator 2.5/0); harness: witness gate fix, server item.script, item.get weight/cookable/lastCookMinute, chef + perk.xp, recipe module scan, gameMinute acks; template fixed for passes 2-3; slice 07 OQ 6 first half closed M, second half C |
| 10 | P3c · Teardown 2 | done | 2026-09-11 | a42c8b4..f2e4e3a | simpleStatus (2867431511) torn down: docs/mods-survey/teardowns/simplestatus.md (measured MP section, every M row reproducing from td2-20260910-231655); one live session: the server-authoritative Nutrition read client-side as a 1 Hz staircase of a decaying server value (a client write overwritten within ~1.5 s), graded with a signed per-snapshot band, client-first reads and driver-side wall brackets; player modData moves whole-table on transmitModData (wipe-and-replace), the client census the superset (server 0 at join / 4 after ~30 s, client 5); the per-frame uncached getNutrition() read pattern (ten calls per weight bar); the display-name control on vanilla foods; harness: tostring float rendering, the three weight flags in nutritionSnapshot, server moddata.set, client text.get (291f977); the client-copy question carried to slice 11 and resolved there (pushed once per game minute while cooking, heat > 1.6f); final fix wave bc87a58 (39 items) + residuals |
| 11 | P3d · Teardown 3 | done | 2026-09-11 | 7dfd3a6..124e7a2 | AutoCook (3388721641) torn down: docs/mods-survey/teardowns/autocook.md (11-row measured MP table; the merge rule measured: the version dir wins a same-relative-path collision with common/ — OUTCOME M n = 1 / MECHANISM C, bounded to a version dir that ships colliding files; closes catalog OQ 1 and 3); one live session td3-20260911-001948 (the Steak carrier probe: below the strict heat > 1.6f gate the client copy of a server-pinned cooked item is bit-frozen while the server decays — the cooking-branch sendItemStats of Food.update is the carrier, reconciling passes 1 and 2; the server->client transmit corroborated wipe-and-replace; the weight flags agree only under the trivial arm, a slice-12 item; the removed-API negative bounded to the window-build path); harness: lua.global (d8cc34e); CleanUI collision: load-order dissolved, runtime pcall half open; final fix wave 8906b79 (25 items) + residuals |
| 12 | P2a · Platform reference | done | 2026-09-11 | f36a860..17a683b | Platform reference written from our own experiment mods: docs/modding/anatomy.md, item-overrides.md, lua-api.md (every mechanism M from a session or C from the jar); eight TKX_* experiment mods under testing/experiments/, eleven profiles, seven live sessions x121-x127 (x121 overrides/eat-hook/nutrient, x122 loader, x123/x123b folder-drift + requested-id, x124/x125 script-body order, x126/x127 pcall/raise probes); findings: a partial item block MERGES per key (the reset is a no-op) so the item pass may ship only the keys it changes; script bodies replay sorted by the stored script path, independent of Mods= position, last body wins per key; the live mod.info chain reads the version dir mod.info first (searchForModInfo is dead code) — one id per folder, folder name irrelevant; an empty version dir costs nothing; a dedicated server resolves no display name and getText never reaches item names; OnEat fires on both sides, a server-side wrapper of ISEatFoodAction.complete runs before Eat; a mod server/ file also runs in the MP client VM; transmitModData replaces server->client and wipes client->server (n = 2); the weight flags agree on both non-trivial arms (no clamp below 0); pcall CATCHES a Kahlua nil call (the standing rule falsified), an unguarded raise aborts only its own handler on the server, and the -debug client parks in the Lua debugger on its first mod error; harness: text.get shared + null guard (ad683fe, 5d9633f) |
| 13 | P2b · Moddability wall map | done | 2026-09-17 | 2019621..91fda27 | Moddability wall map written offline from waves 1-3, slice 12 and a fresh jar read: docs/modding/wall-map.md — 64 rows across ten areas (25 CANNOT / 22 CAN / 14 CAN WITH A WORKAROUND / 3 UNKNOWN, every UNKNOWN naming its experiment), 26 named experiments with owners and costs (21 sessions, ~113 min live); the jar read overturned eight plan predictions: a script-file CONTENT checksum is compared on join and a mismatch disconnects the client (byte-identical script files or a disconnect; role-bypassable — C-only, the map's top experiment X16); registering a new MoodleType and retuning vanilla moodle thresholds are both walls (Moodle.updateMoodleLevel private with a default path; MoodleStat not exposed to Lua); an unrecognised item-block key becomes modData on every instance (a CAN route); mod traits are registrable but carry their effect only in Lua; ZomboidGlobals is snapshotted into Java statics at load (defines.lua replaceable only by file shadow); the hook inventory is closed (8 declared / 6 fired); slice 12's readings reclassified 33 of the plan's 62 seed rows; working reads jar-locks / evidence-harvest / gaps kept in the SDD workspace |
| 14 | P5 · Feasibility notes | ready (needs all; plan in wave 4) | | | |

## Resume notes (in-progress slices)

- **09 · P3b · Teardown 1 — `SKITTLE_LongTermPreservation4220` (item `3774789651`), pass 1 of the 09–11 template.** *(Closed 2026-09-10, `d3715c5..564afb8`, close commit `94d208e`; kept as the pass's record — the two bullets below describe the state at the ledgers commit and the review they name has since run.)* All five tasks are done and committed: `d3715c5` profile → `45c0915` profile fix (module-qualified recipe name) → `37e411e` harness prep → `e3afaa0` the measured session → `4239c5d` driver minors → `903ccaa` a second harness line → `38ecf23` the follow-up session → `f8c6ceb` the teardown doc → this ledgers commit, plus `a16ad09` (`pzt: Profile repr`). On disk: `testing/profiles/teardown-longtermpreservation4220.toml`, `testing/experiments/td1_longtermpreservation4220.py` and `td1b_longtermpreservation4220.py`, artifacts `testing/artifacts/td1-20260910-192457/` and `td1b-20260910-202029/`, and `docs/mods-survey/teardowns/longtermpreservation4220.md` (519 lines; 23-row graded `## MP handling` table).
- Runs: acceptance `run-20260910-191842` (`RESULT: PASS`, 3/3 verify probes), sessions `td1-20260910-192457` (115.9 s) and `td1b-20260910-202029` (96.9 s); `errors=0` on both. Gates at this commit: `doc_lint docs/mods-survey docs/modding` **0**, `doc_lint docs/vanilla docs/modding docs/testing references docs/mods-survey/nutrition-mods.md` **0**, `mod_lint 3774789651` **0 ERROR** (1 INFO `folder-id`), **281** tests.
- **What remains:** the pass's whole-branch review. The controller then flips this row to `done` (date, commit range, one-line outcome naming the mod and the run ids) and pushes — Task 5 does neither. The two artifacts and `testing/runs/` are final for this pass: nothing here is to be re-run. The questions the pass left open are in the ripples below (the display-name discriminator, the client copy that never ticks).

## Ripples (findings that change a later slice's plan)

- (from 02) Item aging (`age`/`offAge`/`freezingTime`) runs only on the server and never travels in `ItemStatsPacket`; `setRotten(true)` and `setFrozen(true)` write dead/undone fields (use `setAge(offAgeMax+1)` / `freeze()`); slice 05's in-game spot checks must read item state on the server.
- (from 02) `ItemStatsPacket` reuses cached packet objects without reset and writes 20 fields only when non-zero while applying them unconditionally: a zero-valued field arrives carrying the previous packet's value. Any slice (or the mod) reading `cookingTime`, `hungChange`, calories/macros etc. client-side on freshly synced items must treat zeros with suspicion; slice 13 (wall map) records it as a platform hazard.
- (from 02) Cooking skill scales extracted macros by (1 + lvl/15) and shrinks hunger by (1 − 0.03·lvl) in evolved recipes — Cooking 10 creates ~17 % of calories; relevant to slice 06's recipe dataset and the item pass.
- (from 02) Measured-artifact skew rule: when a fix round follows the last live run, `testing/artifacts/README.md` discloses the script/artifact delta (applied for exp01 and exp02).

- (from 01) The **server** owns `Nutrition` in MP (client-side `setCalories` is overwritten within ~1 s by the 1 Hz `PlayerStatsPacket`; the eat action completes server-side). Slice 04's scenario must set/feed calories on the server bus (`nutrition.set <user> …`) and read server values; slice 03's decay measurements likewise read the server. `docs/testing/spikes.md` S6, `pipeline-design.md` and `docs/modding/patterns.md` stated the reverse and were corrected in slice 01 (Task 7).
- (from 01) Kahlua `pcall` does not catch "tried to call nil" — harness code must nil-check Java members before calling (`TK.call`); every later slice's Lua follows this — first half falsified, slice 12 (`x126-20260911-045205`); see the (from 12) ripple and `docs/modding/lua-api.md` § 5.
- (from 01) B42 hunger/thirst are read with `Stats:get(CharacterStat.HUNGER/THIRST)` — the route every live snapshot answered with (M). `Stats` carries no `getHunger`/`getThirst` in its 34-method list (C, `./pz.sh methods zombie/characters/Stats`); the harness's `getHunger()`/`.hunger` fallbacks short-circuit and were never exercised, so "gone" rests on the method list, not on the run (slice 03).
- (from 01) Script-level Calories/Carbs/Lipids/Proteins have no Lua getter — slice 05's in-game cross-check must use item instances (`eat`'s `itemBefore`) or count-only checks.

- (from 03) **THIRST level 4 drains health at five times the hunger rate** (`BodyDamage.Update` `/10` vs `/50`, per multiplier unit; 17.82 health/game-hour on the 90-minute fixture, 11.88 on a 60-minute day — day-length dependent). Any scenario or teardown session that runs longer than ~29 game-hours must manage thirst, or the subject dies (slice 04 lost its first run to it). P5 can treat "dying of thirst" as a live vanilla mechanic.
- (from 03) The `FOOD_EATEN` timer is a game-time quantity (`minutesPerDay/30` units per game-second; 11 000 cap ≈ 1.02 game-hours). Scenario authors who rely on the eat gate reason in game time, not real seconds or frames.
- (from 03) Weight-band traits are applied on the server (`applyTraitFromWeight`) and `getKnownTraits()` returns lowercased names; whether they reach the client at all is open (body-stats.md open question 10). Teardowns that probe traits read the server and match case-insensitively.
- (from 03) `StatsDecrease` never touches calories; the only nutrition-side sandbox lever is `Nutrition` on/off (slice 01) — the profile builder (07) needs no calorie multiplier key.
- (from 04) The harness test layer (`TK.test`, `t:at/every/eventually`, `test.run` on the server bus) and `pzt scenario --side server` are the L2/L3 base for every later live slice; scenarios live under `server/scenarios/` and run server-side.
- (from 04) `setCalories` is not food: a scenario that feeds the calorie store must pin hunger and thirst each sample, and `Nutrition.update` stops on a corpse (weight freezes at the death value). The next `nutrition_3day_*` run is the first to exercise the `thirst` sample column and the dead-subject verdict — nothing has yet.
- (from 04) The decoded weight model is verified on the live server to sub-gram accuracy over three game-days (residuals are hourly-sampling bias); P5 and the item pass can rely on it. Under the 3 700 clamp, two +2 000 doses a day lose 4 790 of 12 000 kcal — intake pacing is a balance lever slices 12–14 must account for.

- (from 08) The teardown subjects for slices 09–11 are fixed: `<MOD>` in the template plan `docs/superpowers/plans/09-11-teardowns.md` resolves to **`SKITTLE_LongTermPreservation4220`** (item `3774789651`, pass 1 / slice 09) → **`simpleStatus`** (`2867431511`, pass 2 / slice 10) → **`AutoCook`** (`3388721641`, pass 3 / slice 11), with the fall-through order `SkillRecoveryJournal` (`2503622437`) → `MoodleFramework` (`3396446795`) → `SomewhatTraitsCore` (`3498347699`). The six criteria and the filtering that produced them are in [`mods-survey/nutrition-mods.md`](mods-survey/nutrition-mods.md) § The three picks; no fall-through was used.
- (from 08) A teardown profile names the **engine-resolved id**, not the folder: pick 1's folder is `LongTermPreservation4220` and its id `SKITTLE_LongTermPreservation4220`. Where one workshop item ships several mods (`3498347699` ships 3, `3624538051` ships 5) the profile must carry `id` alongside `workshop_id`, or it names the wrong subject.
- (from 08) Pick 1 settles slice 07 open question 6 on the way past, and the question has **two halves**. NTFS is case-*preserving*, so a run on `simpleStatus` would show which of `SimpleStatus` / `simpleStatus` `harness.install` wrote — that half it can answer. The half it cannot is the behavioural one: Windows path lookup is case-*insensitive*, so the mod loads either way and the run says nothing about whether the rename is **required**. Pick 1's `LongTermPreservation4220` → `SKITTLE_LongTermPreservation4220` differs by a whole prefix, so it answers both, and slice 09 runs it first. `docs/testing/profiles.md` § Open questions 6 is corrected accordingly; no `docs/decisions.md` row had named `simpleStatus` for it.
- (from 08) **Parked harness defect, for slice 09 pass 1 to close while the harness is live:** `witness.moddata item:` or `global:` — a scope word with a trailing colon and no name — matches neither the `^(item):(.+)$` form nor the bare-word gate, so the token is read as a modData KEY on the default `player` scope and the reply is a plausible census of the wrong subject (`scope` and `resolved` disclose it). The fix is the one-line gate `^(player|item|global):?$`; slice 08 deliberately left the Lua alone so that `testing/artifacts/exp08-20260910-152944` stays free of script/artifact skew for a path no probe in it exercised. Disclosed in [`testing/README.md`](testing/README.md) § Command bus.
- (from 08) `witness.fields` / `witness.moddata` exist on **both** sides and are the per-mod instrument for 09–11 and for slice 12's override experiments: no new per-mod harness command should be written before they have been tried. Shapes, the `<id>` grammar, the `missing` / `nils` split, `TK.WITNESS_MAX = 32`, the modData census (`*`, dotted paths) and the "a table on every path but the usage gate" rule are in [`testing/README.md`](testing/README.md) § Command bus; the live `M` is `testing/artifacts/exp08-20260910-152944/` (n = 1). Do not copy the 08 plan's getter list blindly — `getSlicesOfBreadEaten` does not exist on 42.20.4 and `getCalories` is on `Nutrition`, not on `IsoPlayer`.
- (from 08) Item modData readings must name their side. The same instance answers `{}` on the server and `{"customName": "Apple"}` on the client, because `InventoryItem.setCustomName` writes that key from `InventoryItem.load` (six call sites, jar-verified) — so exclude `customName` from any mod-key census, and expect a field sync that carries modData to **wipe and replace** the receiver's table (`SyncItemFieldsPacket.processModData()`). Player modData moves whole-table as well (`transmitModData()` → `ObjectModDataPacket`: an unrelated `hotbar` key rode along in the probe).
- (from 08) **Slice 09 pass 1 runs the item modData census on both sides** (`witness.moddata item:<user>/<fullType> *` on the client as well as the server). **Slice 08 ran the server side only**, so the client half of the cross-side finding rests on one `item_client.fields.getModData` reading, and the artifact README marks `moddata_item` do-not-cite as an independent corroboration.
- (from 08) `data/mod-inventory.json` guards for anything that reads it: `mod_id` is `""` when a mod declares none (1 row), so no consumer may assume a non-empty id; a zero in `stats` / `signals` is trustworthy only when `media_at` does **not** list `common/media` (177 of 230 rows do), and no merge rule is asserted; `script_item_blocks` counts every item definition — clothing and vehicles included — so food items are still counted by hand off the `Type = Food` blocks, though it now **does** strip `/* */` and `//` (slice 08's final fix wave: LTP 17→15, ZVirus 102→86, corpus 6648→6630, and it agrees with `food_scan.parse_script` on all nine script-signal rows); `sandbox_options` now reads `common/media` too (44→55 rows true); `workshop_item_mtime` is a download stamp, not an update date — for a real one read `data/workshop-catalog-details.json`, which has the nine catalogued mods' page dates.
- (from 08) Cadence reminder for every timed teardown scenario: `24 × speed / day_minutes ≲ 8`, and read the run's own `cadence` block before citing any game-clock number (`docs/testing/profiles.md` § The cadence ceiling).
- (from 08) The Workshop holds B42 nutrition mods this machine does not have — `3736275816` "ApocalipseBR - Nutrition Sync Fix", the closest public thing to this library's own subject, and `3796644824`, a third-party patch to pick 1 — while the installed corpus contains essentially **no vanilla food override** (one name collision across the nine script-signal mods: `Horse` redefining `Base.Rope`). Slice 14's feasibility notes should name both; a human unblocks a deeper look by subscribing and re-running `tools/mod_inventory.py`.

- (from 09) **The item wire is `ItemStatsPacket`, and its field list is the contract.** The packet puts **43** fields on the wire, of which **39 are item state** — the other four (`containerId`, `id`, `isFluidContainer`, `isFood`) are addressing and presence flags that are never applied to the item, which is the reconciliation [`vanilla/food-item-model.md`](vanilla/food-item-model.md) § MP behaviour already carried; **slice 02's 39 stands and was not overturned**. (The field names are in [`mods-survey/teardowns/longtermpreservation4220.md`](mods-survey/teardowns/longtermpreservation4220.md) § MP handling.) Measured on one instance held by both sides at once: the five nutrition writes (`calories`/`proteins`/`lipids`/`carbohydrates`/`hungChange`) arrive intact, while `offAge`, `offAgeMax`, `isCookable` and `isCustomWeight` **never** arrive and nothing later repairs them. Slice 12's platform reference should prove the list itself; slice 13's wall map gets "writes an item field outside the packet" as an unsynced surface. A mod's **script** half needs none of this — `item.script` read on both sides is identical field for field (M, `td1-20260910-192457`), so script-declared item data is correct on both sides for free while Lua-written data is only as good as the packet.
- (from 09) **A server-side write can be *discarded*, not merely desynced.** `Food.getActualWeightUnmodded` returns **0** whenever `getDisplayName()` equals `getFullType()`, and flipping `customWeight` moves the server's own getter onto that arm — so LTP's 0.35 kg read back as 0 on the server and 0.35 on the client, the authoritative side holding the wrong value. Rule for every later slice: **check a getter's guards before trusting a field you wrote**. The discriminator is **OPEN**, with **three** unseparated hypotheses: (i) **no translation entry** for `Skittles.CuredPork` in whatever table `InventoryItem.name` resolves from; (ii) the **B41-layout file is ignored** — LTP ships `42.20/media/lua/shared/Translate/EN/ItemName_EN.txt:3` (`ItemName_Skittles.CuredPork = "Cured Pork"`), the B41 layout, while 42.20.4 ships translations as `ItemName.json` (`tools/food_scan.py:270-271`), so the mod's name may never be loaded at all; (iii) **mod translations are not consulted** for `InventoryItem.name` on a dedicated server. n = 2, and the control must come from outside LTP, whose only two `DisplayName =` lines sit inside a commented-out block; neither remaining pick supplies one (`simpleStatus` and `AutoCook` both have `script_item_blocks = 0` in `data/mod-inventory.json`). **The criterion, corrected:** it is *not* "a vanilla food with no `DisplayName =` line" — **no vanilla food has one** (0 of the 1 005 records in `data/food-items.json`; 0 occurrences in `media/scripts/generated/items/food.txt`, 2026-09-10), so that test selects everything. The discriminator is the **translation table**: the six `module Base` foods with a null `display_name` — `Base.FruitSaladClay`, `Base.HotDrink{Copper,Gold,Metal,Silver,Tumbler}` — are exactly the ones **absent from** `media/lua/shared/Translate/EN/ItemName.json` (4 889 entries), while `Base.Steak` **is** in it as `"Steak"`. **Slice 10's session reads `getDisplayName` on those six beside `Base.Steak` on both sides**; pass 1 does not run it.
- (from 09) **The dedicated server ticks an inventory item about every 5 s, not every frame** (bounded three ways, reproduced), because `InventoryItem.calculateTimeMultiplier`'s server arm is real-time-delta driven and clamped at 6 s; heat decays ≈0.036/s at `mult 4.7961`. Two consequences for slices 10–12: a timed probe must not assume per-frame item updates, and **a bus call cannot be made the cause of a server-side item transition** on this fixture (each call costs ~1 s round trip, the game-minute gate reopens every ~3.7 real seconds, and the tick wins the race). Therefore **read the guard state before the last `item.set` that opens a gate**, never after — a guard read taken afterwards grades "the server's tick won" as "it was already done at spawn".
- (from 09) **A client's copy of a server-spawned item may not tick at all.** Over 11.1 s the client's `heat` and `cookingTime` were frozen at the last pushed values while the server ran two or three ticks — so a client-side reader sees the last push, not a live simulation. M on the freeze (`td1-20260910-192457`), **mechanism open** (it is not `updateInternal`, not `recursiveItemUpdater`, not `Food.update`'s own gates; the call path carries no side guard). This qualifies the assumption `02-notes` leaned on. **The next check, for slice 10's pass** (whose subject `simpleStatus` is a pure client reader): a client-side `witness.fields` of the item's **container** — is the instance the client answers about actually held by the local player's inventory object, or a detached copy? — plus the client's `heat` / `cookingTime` read **twice 10 s apart** on an item the client itself holds. Those two separate "never scheduled" from "scheduled on a different object"; pinning `heat` on a client-held item and watching it decay, or not, is the same probe from the other end. Slices 12/13 should not describe client-side item simulation until it has run. **Superseded 2026-09-11 — see the `(from 11)` ripple below.** The mechanism is no longer open for the *cooking* case, and this ripple's parenthetical is exactly wrong about it: the discriminator **is** inside `Food.update` — its `heat > 1.6f` cooking gate — and the carrier is that branch's per-game-minute `sendItemStats`. This reading is the **below-gate arm**; slices 12/13 may now describe client-side item state **with the gate named**.
- (from 09) **Cooked food's `thirstChange` halves on every server→client hop** (0.2 raw → 0.1 on the wire → 0.05 on read): `ItemStatsPacket.setData` sends the cooked-ladder getter while `applyItemStats` stores it as the raw field. It is **one halving per server→client hop and it converges** (a second push left the client at 0.05 with the server unchanged at 0.1), because there is no client→server item-stats hop. A **vanilla** defect that lands on any mod cooking anything in MP — ours included. Canonical graded row: [`modding/patterns.md`](modding/patterns.md) § Measured MP sync facts → *The other direction*; [`vanilla/eating-pipeline.md`](vanilla/eating-pipeline.md) § MP behaviour cross-references it. Slice 13's wall map takes it as a platform hazard, slice 14 as a balance caveat: a thirst number a player sees may be half the one the server applied.
- (from 09) **Item-modData censuses in slices 10/11 exclude `customName` AND `Tooltip`.** Slice 08's one-key exclusion is short: any script `Tooltip =` line becomes an item modData key on **every** side that instantiates the item, through `InventoryItem.setTooltip`'s `getModData():rawset` (C, jar) — so its presence on both sides is not evidence of a sync. Exclusion sets are **per scope**: the item set applied to a player census reports the four vanilla fitness keys as findings. Better still, take a vanilla control item's census in the same run (`td1b-20260910-202029` did: server `keyCount 0`, client `customName` only, `Tooltip` only where the script declares one).
- (from 09) **`witness.moddata` takes keys SPACE-separated behind an explicit scope** (`player:<user> k1 k2`, `item:<user>/<fullType> k1 k2`, `*` for a census). A comma-joined list is read as one key named `"k1,k2"` and answers a plausible census of nothing — the template's driver skeleton had that defect; the plan now sends a pass to the shipped `testing/experiments/td1_longtermpreservation4220.py` instead, which sends the two scopes separately and gets the grammar right. `witness.fields` is the opposite: one comma-joined, whitespace-free token. Do not share a join between them.
- (from 09) **The harness is not frozen for a teardown pass** (standing rule, 2026-09-10): a pass adds the surface its probe plan needs, jar-confirmed first, in its **own commit ahead of** the session commit with `docs/testing/README.md` updated in it, with the acceptance run as the smoke test. Pass 1 added six commands/fields across `37e411e` and `903ccaa`. The exception is a change that reshapes existing evidence: `TK.json` rendered every non-integral number with `%.6f` at pass 1, so ulp-level cross-side comparison was unobtainable and no claim of "bit-equal" can be made — **the first harness change of slice 10 widens that format to full float precision** — planned here as `%.9g`, or the widest form Kahlua's `string.format` actually accepts, **measured on the bus before it is relied on**. **What shipped (`291f977`) is `tostring(v)`, not a `%g`**: Kahlua implements its own `string.format`, so no C `%g` guarantee applies, while `KahluaUtil.numberToString` is `Double.toString` for a non-integral double — a language-guaranteed exact round trip that needed no measurement. It is disclosed as a rendering change in **two** places in the same commit: `docs/testing/README.md` (the command bus's number format) and the skew section of `testing/artifacts/README.md`. `td1`/`td1b` and everything older stay as recorded.
- (from 09) **Feasibility constraint for slice 14:** LTP adds 14 foods in its own `module Skittles` with **zero** vanilla name collisions, so an item pass scoped to `module Base` will not touch them — our mod must decide explicitly whether mod-added foods are in or out of its balance pass, and say so. It is also a reminder that the corpus still holds **no precedent** for re-declaring vanilla `module Base` foods at scale (§ Open questions 8 of the catalog), which is the mechanism our item pass needs.
- (from 10) **A scriptless, UI-only mod still has a tier-(a) `[[verify]]` probe: its own translation keys.** `text.get <key>` (client since `291f977`; shared since `ad683fe`, slice 12) calls the Lua global `getText`, and a **miss returns the key itself** — so a mod-defined prefixed key that vanilla does not declare, whose value is not a substring of the key, cannot pass unless the mod's translation table actually loaded. `simpleStatus` moved (b) → (a) on `IGUI_SS_BARTITLE_HAPPY` → `"Happiness"` (`run-20260910-230752`), and slice 11's `AutoCook` took the same route on `UI_AutoCookMode`. Any later pass on a mod with no scripts and no bus-reachable state starts here rather than at a console grep — and keeps the console grep beside it, since the two fail differently.
- (from 10) **A cross-side comparison is graded on a SIGNED, per-snapshot band, and the driver must bracket each side's read itself.** The server's copy is a ramp and the client's a 1 Hz staircase read ~1 s later, so the sign of `client − server` is a property of the **driver's read order**, not of the game: read client-first (pinned) and the macro band is `[r·δ_min, r·(δ_max + 1 s)]` with `δ_min = t_srv_before − t_cli_after`, `δ_max = t_srv_after − t_cli_before`; read server-first and the same healthy mirror reports ≈ −0.26 kcal. Weight is **opposite-signed** (the client never recomputes it) and needs a one-float32-ulp floor. Two hard requirements for slices 11–13: **wall-bracket every `stats.get` / `nutrition.get` on both sides** (pass 1's driver did not, so δ was unobtainable; `td2_simplestatus.py` does), and **report the raw gap beside the snapshot's own δ** — a bare gap is uninterpretable. A snapshot inside a write's push window is a **latency reading, not a band test**.
- (from 10) **The display-name discriminator is answered inside vanilla, and no later pick can improve on it.** `Base.FruitSaladClay` (absent from `ItemName.json`) reads `getDisplayName() == getFullType()` and `getActualWeightUnmodded() == 0` on **both** sides, while `Base.Steak` reads `Steak` and keeps 0.3 (`td2-20260910-231655`) — so hypothesis (i), *no translation entry*, is sufficient on its own and (iii), *mod translations are not consulted on a dedicated server*, is not required to explain pass 1's symptom. **(ii) stays open by design**: separating "42.20.4 never loads a B41-layout `ItemName_EN.txt`" needs a write into the read-only mod tree. `AutoCook` supplies no mod-side control either — the `script_item_blocks = 0` reading for **both** remaining picks is in the `(from 09)` ripple above and is not repeated here; slices 12/13 either accept (i) or budget a mod-tree write.
- (from 10) **A client's copy of a server-spawned item DOES move — pass 1's freeze row is REOPENED, not confirmed.** After a server `item.set heat 2.0`, the client's `getHeat` / `getCookingTime` went 2 → 1.697029948234558 and 0 → 0.1182333305478096 over 10.5 s and were **bit-identical** to the server across a 0.5 s read offset that an independently ticking copy would have broken. "Push-updated" is the parsimonious reading, not a proven one; "both sides tick and converge on the same float32" is not excluded. The canonical row (`modding/patterns.md` § Measured MP sync facts) is marked **CONTESTED** with both readings quoted, and the discriminating probe — pin `heat` at **1.2**, below `Food.update`'s `heat > 1.6f` cooking gate, then read both sides twice 10 s apart — is named for slice 11. **That probe ran on 2026-09-11 and the row is now `resolved per arm`, not CONTESTED** (the `(from 11)` ripple below); this ripple records the state at slice 10's close.
- (from 10) **The bus renders non-integral numbers with `tostring` since `291f977`** — `KahluaUtil.numberToString` is `Double.toString` for a non-integral double (an exact, language-guaranteed round trip) and `String.valueOf((long) d)` for an integral one, so integers still arrive as integers and no key changed name or type; `±Inf` now answers `null` (it used to produce an unparseable ack and lose the whole reply). Measured on the bus: **1 225 JSON number tokens** in `td2`'s artifact (871 non-integral, 354 integer), **0 non-round-tripping** — the **1 292** first recorded has no recorded provenance and does not reproduce as any census of the file (`\d+` gives 2 339) — cite 1 225. **Everything written before `291f977` is `%.6f`** and its ulp-level claims stay unobtainable. What the widening bought immediately: `Nutrition.save`'s `d2f` / `load`'s `f2d` narrowing is now visible on the wire — the client's weights are exactly float32 (6/6), the server's are not (5/5).
- (from 10) **The anti-pattern to name in the platform reference: a per-frame, uncached read of a server-owned store.** `SSBar:prerender()` calls `prepareBarInfo()` every UI frame (`ISSSBar.lua:451-470,464`), which calls `bar.valueFn(player)` (`:171`) and re-enters it up to three more times per bar (`:236-238`) — against a source that changes at **1 Hz**. It costs up to four Java round trips per bar per frame to re-read a number that cannot have moved. Rule for our own UI and for slice 12's reference: cache on the push, not on the frame. (Closed `patterns.md` § Open pattern questions, slice 10.)
- (from 10) **A client runs `updateWeight`'s flag half and stops exactly at `setWeight`.** `isIncWeight` / `isIncWeightLot` / `isDecWeight` agreed on both sides at all six snapshots **and** with the arm recomputed from that snapshot's own macros, so the flags are correct on an MP client even though the client's weight is a staircase — the `GameClient.client` skip (`@317-@320 L198`) sits *after* the flag writes. Two corrections ride with it: `setIncWeightLot(true)` fires on the ×2 arm too, so the flag starts at carbs **or** lipids **> 400**, not 700 (`@194-@226 L179-L181`; only the 700 arm was exercised live), and the agreement carries a hidden dependency — both trait lists were **empty**, so body-stats open question 10 (do weight-band traits reach the client?) is untouched. Anything of ours that keys on a weight band waits for that row.
- (from 10) **`player:transmitModData()` is a whole-table wipe-and-replace, now measured.** A key planted only on the server (`pzt_ss_server`) was **gone** from the server's census after the client transmitted, while the client's own keys and the vanilla `hotbar` arrived: the receiving side's table becomes *exactly* the sender's (`KahluaTableImpl.load @5-@6 L333` wipes before it rawsets). Slice 13 takes it as a platform hazard and slice 14 as a design constraint: **never keep server-authoritative state in player modData on a server whose clients also transmit** — the mod that loses the data is never the mod that transmitted it (`patterns.md` FILTER 10).
- (from 10) **The server's player modData is EMPTY at join.** On `td2-20260910-231655` the server answered `keyCount 0` at 79.2 / 91.1 / 97.4 s and **4** (the vanilla fitness/strength keys) only from 108.8 s — 22–33 s after `session_ready` — while the client held 5 (those four plus `hotbar`) throughout. Pass 1's "server 4 / client 5" is a **late-session** reading; any census taken at join sees server 0 / client 5, and an exclusion set built from it would be wrong. The conclusion it was used for is unaffected (the client is the superset either way).
- (from 11) **The `common/`-vs-version-directory merge rule is measured, and it is the wiki's: version-wins.** The version directory's file **wins** a same-relative-path collision, `common/` supplies everything the version directory does not ship, and **both end up in one Lua state** — outcome **M** (`td3-20260911-001948`), mechanism **C** (`ZomboidFileSystem.loadMod` maps `commonDir` then `versionDir` with an unconditional `activeFileMap.put`, `L758`/`L773`). Five readings agree: the loader's own five `overrides` lines (two translation JSON over vanilla, three client Lua over `common/`, no `.png` tail); the load-time modData key; the translations, which merge and leave vanilla intact; `lua.global` finding both 42.13-only discriminators live on the client; and the arithmetic — `_G.AutoCook` has `keyCount 49` (= 23 scalars + 15 + 4 + 7 functions) where common-wins gives 47. **The bound goes with the sentence**: it is measured on a mod whose `getVersionDir()` resolves to a tree that **ships colliding files**, n = 1. **Slice 12's platform reference should prove the rule itself** (the map, the dedupe, `Translator.tryFillMapFromMods`' merge) rather than re-deriving it per mod; it closes [`mods-survey/nutrition-mods.md`](mods-survey/nutrition-mods.md) open questions 1 and 3, and the graded statement lives in [`modding/patterns.md`](modding/patterns.md) KEEP 10.
- (from 11) **A mod's entry point can live entirely in `common/` while its live version folder registers nothing.** AutoCook's live `42.13/` tree contains **no** `Events.*.Add` at all — its inventory row's `top_events` is `[]` and that is *correct for what it scans* — while the only registration, the only vanilla patch and the whole translation set sit in `common/`. The shape to recognise: `common/` hosts the hook and the version folder overrides the behaviour it calls into. Anything in slices 12–14 that infers "this mod hooks nothing" from a live-folder sweep is reading half the mod.
- (from 11) **`data/mod-inventory.json`'s `signals` / `stats` / `top_events` describe the live version folder only, and here is the size of the gap in one row:** for AutoCook they miss **7 of 10 Lua files, 1 238 of 2 155 lines**, the mod's only event registration, its only vanilla patch and its whole translation set. `media_at` remains the guard on a zero (177 of 230 rows list a `common/media` the scan does not read), and that guard is now backed by a measured merge rule rather than by an open question. Widening the scan is a tools-sized change and was deliberately not made here; [`../data/README.md`](../data/README.md) does not yet carry the consequence, which is the ripple **(12, 13)**.
- (from 11) **Two API removals in 42.20.4 are a seed for slice 13's B41→B42 breakage list:** `IsoGameCharacter.HasTrait(String)` and `InventoryItem.getTypeString()` are gone from the jar, and the only code that still calls them in this corpus is a mod's **shadowed** `common/` copy — where the call never executes, so it never raises. Zero raises in a client console is therefore evidence about **which copy ran**, not about whether the code is safe; a wall map that lists removed members should say where a dormant call can hide.
- (from 11) **Compatibility for slice 14, measured cold on a neighbour:** `CleanUI` (3437629766) forks `ISInventoryPaneContextMenu.lua` and wraps the `OnPreFillInventoryObjectContextMenu` trigger in `pcall` plus a printed failure line — and that trigger is AutoCook's **only** entry point. Two qualifications the static read produced and a run must not be asked to repeat: the **load-order** question dissolves (a mod file at a vanilla relative path runs in vanilla's slot, because `LuaManager.LoadDirBase` adds vanilla's block first and dedupes by relative path, so CleanUI wins in either order), and the collision is **version-dependent** — CleanUI ships that file in only **3 of its 7 trees** (`42.15`, `42.16`, `42.19`). What is left open is the **runtime** half, and it needs a `triggerEvent` route the bus does not have: booked for slice 12 (the harness) and slice 14 (the compatibility note).
- (from 11) **The freeze-vs-push contradiction is settled, per arm: `Food.update`'s cooking-branch `sendItemStats` is the carrier.** Pinned **below** the 1.6 cooking gate (`heat 1.2`), the client's copy of a server-spawned `Base.Steak` was **bit-frozen** — `1.2000000476837158` at both reads, 12.54 s apart — while the server's decayed to the 1.0 floor (`td3-20260911-001948`); pass 2 pinned 2.0, **above** the gate, and its client copy moved bit-identically to the server's. So pass 1's freeze and pass 2's movement are the same mechanism read on either side of one gate. **M per arm** (three arms, three sessions, n = 1 each), **mechanism C**, and the refinement matters: below the gate the client's copy does not tick **at all**, which narrows rather than proves the "it can tick" clause. Bounds: one item, one 12.54 s window, one fixture; frozen and non-cookable items and every other push path are untested. Slices 12/13 may now describe client-side item state — **with the gate named**. Canonical row: [`modding/patterns.md`](modding/patterns.md) § Measured MP sync facts → *The other direction*.
- (from 11) **A mod that never calls `transmitModData()` still leaks its whole client-only table to the server — the moment any *other* mod on that client transmits.** AutoCook has nine `getModData()` sites per tree and **zero** `transmitModData` calls, yet one harness transmit pushed its nested `AutoCook` table (and `hotbar`, and the planted probe key) server-side, after which the server's census was *exactly* the client's. That is FILTER 10's wipe-and-replace measured on a second subject, and it is a design constraint for slice 14: **player modData is a shared, last-writer-wins surface between mods on the same client**, so neither "my keys are client-only" nor "the server's copy is mine" survives contact with a neighbour.
- (from 11) **A client-side `overrides` grep must expect `2 × n`, not `n`.** The client console prints the whole per-mod `overrides` block **once per Lua state** — once for the main-menu state and again after the join — with identical tails in identical order, while the server log prints it once. A driver that caps the grep at the predicted count silently truncates the second block; cap at `≥ 2 ×` predicted plus headroom. **Aimed at slice 12's driver work** — the first thing to grep a client console since the rule was measured. (The rule is in [`docs/testing/README.md`](testing/README.md) § How a driven client is controlled, in the **Observation** bullet.)
- (from 11) **Booked for slice 12: `AutoCook:allowSpice`'s weight-flag gate is unsettled, and the observed agreement is trivial.** `isIncWeight` / `isIncWeightLot` / `isDecWeight` read `false` on both sides at all four snapshots — but the character sat in `Nutrition.updateWeight`'s **none** arm (weight 80 ⇒ gain threshold 1 000, loss threshold 0, calories running 798.5 → 782.9, strictly between them), where both sides answer `false` whether or not the client recomputes anything. Settling it needs a session that drives calories above 1 000 or below 0 and re-reads both sides. Do not cite the agreement as confirmation of pass 2's flag-mirroring finding on this run.

- (from 12) **For slice 13 — the wall-map inputs are already written, one line per entry area:** `docs/modding/anatomy.md` § Inputs for the wall map classifies all nine (new nutrient fields **CAN WITH A WORKAROUND**; eat hooks **CAN**; the weight formula **CANNOT** client-side but **CAN** for direction; moodles **UNKNOWN**; sync — item stats **CAN**, player stats **CANNOT** client-side, modData **CAN WITH A WORKAROUND**; the cooking pipeline **UNKNOWN**; traits **CANNOT** be read client-side with confidence; translations **CAN**, client-only and JSON-only; the merge rules **CAN**) with the artifact key or jar site behind each and the probe that would settle the rest. Start there, not from the plan-13 seeds — `.superpowers/sdd/13-wall-map/slice-12-outcomes.md` lists which seeds this slice overturned.
- (from 12) **Four checks slice 12 recorded rather than ran, each one boot and each owned by 13.** (i) **CleanUI × `triggerEvent`, both orders in one boot**, carried from the AutoCook teardown § Compatibility notes — CleanUI's live tree is `42.19/`, which does ship the fork, so the collision is live and its runtime half stays **C**; run it together with a `pcall` of a nested `triggerEvent`, and gate it on the **server**. (ii) The **server→client *item*-modData direction**: slice 12 measured both *player*-modData directions and neither merges, but item modData across the same hop is unmeasured. (iii) The **client's own mod-list call site** (`ChooseGameInfo.getModDetails` from the mod selector) — every loader reading of this slice is the dedicated-server `Mods=` path. (iv) The **`ItemType`-omitted partial block**: R2's shipped narrowing keeps `ItemType` and `DisplayCategory`, and what a block omitting `ItemType` does to an existing item is untested. Two more are named in patterns.md: the **id-vs-script-path** boot and a folder whose **only** `mod.info` is `common/mod.info`.
- (from 12) **For slice 14 — the item-pass mechanism is settled and it inverts the plan's assumption.** A second `item` block **MERGES per key**: a narrowed `item Orange` declaring only `Calories` kept vanilla's `hungChange` −0.12 and `carbohydrates` 16.27 (**M**, `x121-20260911-030023` → `phases.M2.items`, verdict `verdicts.M2b`). So **restate only what you change** — everything omitted survives — and a redefinition **replaces rather than adds** (`module Base` stayed at 722 foods across every redefinition boot). Load order between two mods is **sorted by the stored script path** (**C**), **`Mods=`-independent** (**M**, two permutations + `x121`), **last body wins per key** (**M**, `x125-20260911-042055`). R3's consequence for translations: an item's display name is a **client-only** value — a dedicated server resolves none at all, and `getText` never reaches an item name on either side — so gate a translation-only feature on an `IGUI_` / `UI_` key and never branch on a display name server-side.
- (from 12) **Compatibility constraint for 13 and 14: a mod's `media/lua/server/` files execute in the MP CLIENT's Lua state too.** Three independent witnesses in one session (`x121` → `phases.M7.mod_globals.client`). "Only my server file writes this" is false until an `isServer()` guard is written, and a `server/` file that mutates shared state, installs a wrapper or registers a handler does it **twice per MP session**. Resolve the side at runtime (`if isServer ~= nil then …` first — the global may be absent), never from the folder. **M**, n = 1 session, **mechanism untraced**; the probe that settles it is named in `docs/modding/patterns.md` KEEP 12 and is 13's.
- (from 12) **`transmitModData()` is wipe-and-replace in BOTH directions — n = 2 on the client→server half.** A key planted server-side only was gone from the server's census 1.27 s after the client transmitted (`x121` → `phases.M9`, with `td2-20260910-231655` as the first subject); in the other direction the client lost `hotbar` and its own key, gained the server's, and the residual difference set came back `[]` (`phases.M6`). Neither direction merges: **one owner per value, or keep the value out of player modData.**
- (from 12) **The library's standing Kahlua rule was half wrong and is now owned by one document.** `pcall` **catches** a nil call — `false, "tried to call nil java.lang.RuntimeException"`, both sides (`x126-20260911-045205`) — so the "escapes `pcall`" half is **falsified**; an **unguarded** nil call does abort the rest of its own handler's body, while the handlers **behind** it still run (`x127-20260911-052049`, server VM). The `TK.call` / index-first guards stay because a caught nil call **names nothing**. `docs/modding/lua-api.md` § 5 is the owner; `patterns.md` FILTER 11, `docs/testing/README.md` and the `PZTestKit_Core.lua` header now cross-reference it. The harness comments were brought to the measured rule in `7dcb37a` + `96a777f`, and the teardown's last stale line with them; the frozen drivers / probe mods keep their historical wording by design.
- (from 12) **A driven `-debug` client is stopped dead by the first mod Lua error that reaches `KahluaUtil.fail`.** It parks in the Lua debugger's modal break (`L95`–`L97`, `debugBreakpoint`; the server never breaks, `L1183`) — measured on two boots, client 1 × `:96` / 0 × `:100` against server 356 × `:100` / 0 × `:96` (`x127`). **Any slice driving a client must gate a probe that can raise on the SERVER side**, and should set `[client] timeout` low in the profile: a client-side `[[verify]]` row never runs when the client hangs, and `x12-raise.toml` left the 300 s default, so a five-second hold cost 384 s. Guarded code is safe; a **release** client is unmeasured.
- (from 12) **Before quoting any slice-12 number, read the run's block in `testing/artifacts/README.md`.** Seven blocks, each with a skew statement (all seven runs are skew-free — driver and artifact in one commit, never edited after the run) and a ***do not cite*** table. The traps that will bite a careless citation: every `text.get` reading of `x121` (wrong key form, control missed), `x122`'s `phases.L1` (the `Mods=` id and the folder name matched, so it separates nothing), `x123`'s null P17 clauses, the `means` strings in `x124` and `x125` ("alphabetical by id" — the sort key is the **stored path**, and id / folder / path / display name are confounded), `x126`'s `server_error_count: 4` (four harness echoes, **zero** engine faults) and **every client-side reading of `x127`**.
- (from 12) **`server_errors` is a classifier's output, not a fault count** (`testing/pzt/server.py:15` `ERROR_RX`, applied at `:253`). It files a bus reply that merely *carries* exception text as a fault, and it counts every trace line of a genuine dump separately (ten entries per Lua error). Read the list before quoting the number. No code change was made this slice — the rule is in `docs/testing/README.md` § Observation and the reasoning is in the decisions ledger.
- (from 12) **The workshop corpus drifts under the library's feet.** At 2026-09-11 04:47 Steam rewrote one item's `mod.info` mid-slice, so `mod_lint`'s sweep moved from `84 finding(s): 3 ERROR, 30 WARN, 51 INFO` to `83 …, 50 INFO` and the drifting-folder count from 52 to **51** within the same day. `data/mod-inventory.json` is a **2026-09-10 17:47 snapshot** and stays one; every count quoted from it carries that stamp, and `anatomy.md` § 7 carries both sweeps. `tools/mod_lint.py`'s module docstring still quotes the 2026-09-10 sweep (correctly dated) — re-stamp it whenever that file is next touched.
- (from 12) **`ZomboidFileSystem.searchForModInfo` is DEAD CODE on 42.20.4, and the model built on it was wrong.** It is referenced by nothing but its own recursive call, so "registers every `mod.info` it meets and returns the first whose id matches, in `File.list()` order" describes **no live call site**. The live chain is `loadMods → loadModAndRequired → getAvailableModDetails → getModDetails → readModInfo → readModInfoAux`, and `readModInfoAux` opens `<versionDir>/mod.info` **if it exists**, else `<commonDir>/mod.info`, and parses **that one file and nothing else** (`docs/modding/anatomy.md` § 2 and § Code map). `getAllModFoldersAux`'s `common/`-first test is **discovery**, a gate, not the id read. `docs/testing/profiles.md` § Open questions 1 now closes on **that** chain, and `mod_lint`'s `mod-info-place` WARN and docstring carry the bounded measured statement (`df30c75`). Any wording anywhere that says "the first `mod.info` found wins" or "B42 reads the version folder first" as an *engine* claim is wrong as stated.
- (from 12) **`versionMin` / `versionMax` are hard availability gates, and they surface as the wrong error.** `Mod.isAvailableSelf @0-@48 L667-L676` returns false when `versionMin > build`; `getAvailableModDetails` then returns null and the server prints `required mod "<id>" not found` — the same line a missing folder produces. 154 installed mods declare `versionMin` and 13 declare `versionMax` (**C**, `docs/modding/anatomy.md` § 1). A wall-map or feasibility note that reads "not found" as "not installed" will misdiagnose a build gate; and every mod **we** ship must declare `versionMin` deliberately.
- (from 12) **Two mods (or a mod and vanilla) at the same RELATIVE script path do not both load — one file wins.** `docs/modding/item-overrides.md` § Load order (a) states it, and it is **C only**: no session has shipped the same relative script path from two mods. `LuaManager.LoadDirBase` adds vanilla's block first, so a mod file sitting at a **vanilla** relative path runs in vanilla's slot. Consequence for our item pass: give every script file a **mod-unique relative path** (`media/scripts/<ourid>_items.txt`, never `media/scripts/items.txt`). The untested arm belongs to 13/14.
- (from 12) **The mechanism behind "`getText` is not the route to an item name".** `Translator.getTextInternal @0-@830 L420-L498` is a **prefix router** over 25 key prefixes (`UI_`, `IGUI_`, `ContextMenu_`, `Moodles_`, `Tooltip_`, …) and **`ItemName_` is not one of them**; the item map is reached only by `getDisplayItemName(fullType) @0-@38 L589-L595`, which looks up the **raw full type** with ` ` and `-` folded to `_` (**C**, `docs/modding/anatomy.md` § 5). So the nine measured misses are explicable rather than merely unexplained, and a translation-gated feature keys on an `IGUI_` / `UI_` string.
- (from 12) **`OnCooked` and `OnEat` resolve by two different routes, and only one of them takes a dotted name.** `Food.update @627-@718 L462-L467` does `LuaManager.env.rawget(getOnCooked())` and, when the name contains a `.`, splits on the dot for a two-level `rawget` — so `MyTable.myFunc` works for `OnCooked`. `OnEat` goes through `getFunctionObject` and needs a **plain global** (**C**, `docs/modding/lua-api.md` § 3). Do not assume one namespacing convention covers both script hooks.
- (from 12) **Three corpus counts this slice corrected, all dated 2026-09-11.** **4** installed folders (not 24) have `common/mod.info` as their *only* `mod.info` — the 24 in `data/README.md` is a different fact (mods with no `<live>/media` at all); **no** installed folder lacks a version dir (all 230 have one), which is why `mod_lint`'s `version-dir` ERROR has never fired on the corpus; and the lint chain and the engine chain open the **same file on 229 of 230**, differing only on `3396446795/MoodleFramework`, where both candidates declare the same id so **no id moves**. Sources: `docs/modding/anatomy.md` § 2, § 7 and § Discrepancies 1.
- (from 13) **The map's 25 `CANNOT` rows are slice 14's hard constraints — a feasibility note designs *around* them and names the row it is designing around.** The four that change the shape of the mod: **new nutrient fields** (A1 — `Nutrition` is a closed 26-member value object with a fixed five-float `save`/`load`, and no `setNutrition` exists jar-wide, so the object can be neither extended nor substituted); **the weight formula** (C1 — the three macro decay rates are hard-coded Java floats; C4 — `updateWeight`'s `GameClient.client` skip sits before `setWeight` and before `applyTraitFromWeight`); **moodles** (D1 — a registered `MoodleType` is *pinned at level 0* by `Moodle.Update`'s shared tail into the private `updateMoodleLevel`; D2+D3 — `MoodleStat` is absent from `LuaManager$Exposer.exposeAll`, so the public threshold setters are unreachable); and **the two packet walls** (E1+E2 — `ItemStatsPacket` is seven members, 43 fields, no seam; E7+E12 — `PlayerStatsPacket.write` is an unconditional full snapshot pushed at 1 Hz). Each cell names its jar site: [`modding/wall-map.md`](modding/wall-map.md) § The map.
- (from 13) **The 14 `CAN WITH A WORKAROUND` rows' *Residual risk* cells are the risk register for every feasibility note.** A note that leans on one of those rows carries that cell's residual **with its bound**, verbatim — A3/E9/E10 (someone else's `transmitModData()` wipes the table), A6 (the server→client item-modData direction is unmeasured and that run's `verdicts.M7` is do-not-cite), C2 (which of the three arms actually freezes with `Nutrition = false` has never been measured), D4 (MoodleFramework's three unmeasured legs), G1+G6 (a registered trait carries zero Java effect and its sync path is untraced), I2 (the whole file-scope `ZomboidGlobals` branch is C), I9 (mechanism untraced), I14 (server VM only). Dropping a bound is the one defect this map can inflict on 14.
- (from 13) **§ Named experiments is the open-work list, and it carries an owner column.** 26 rows · 24 boot-wanting · 21 sessions · ≈ 113.5 min live · 4 desk reads · 6 harness changes · ≈ 42 h. **Slice 14 owns four** — X4 (does any packet carry `CharacterTraits` to a client?), X5 (does a mod JSON displace a vanilla translation key?), X13 (is the drink path interceptable?) and X29 (MoodleFramework) — and slice 14's job is to say **which `X` rows a design must settle before it is buildable and which may stay open**, not to run them all. Nothing in the map obliges a boot.
- (from 13) (14) **§ What the library cannot yet measure bounds what any feasibility claim may assert.** Nothing on the bus executes a craft, so the cooking pipeline stays **C**; no bus route reaches an arbitrary event dispatch; the `getText` session carried no in-run positive control; `witness.fields` reads zero-argument getters only and caps at 32; one fixture, one admin character, `DayLength = 4`, most rows **n = 1 session**; artifacts older than `291f977` render floats at `%.6f`; modData persistence across a save/load, the Lua drink-driver path and a deliberate client/server script mismatch have **never been run**. An evidence limit is not a platform wall and must not be quoted as one.
- (from 13) (14) **Deployment constraint — the script checksum gate is real, and it is a disconnect.** `ScriptManager.Load @680 L1525` feeds **every loaded script file, mod files included**, into one CR-normalised, load-ordered **content MD5**; the client arm (`NetChecksum$Comparer.update`) force-disconnects with a `kickReason` and the server arm (`AntiCheatChecksumUpdate.update`) acts after a **60 s** grace. So the item pass must ship **byte-identical script files on both sides** (CR/LF differences are tolerated, nothing else is), a server-side-only install of a script-bearing mod is not a deployment option, and a role holding `Capability.BypassLuaChecksum` is the only exemption. **Every arm is C and unmeasured** — wall map J3 `-> X16`, the map's highest-consequence experiment.
- (from 13) **Testing constraint — the `-debug` client break bounds what 14 can ask for.** A raising probe must be gated on the **server** with `[client] timeout` low; a **release** client cannot be launched today, because `debug` is not threaded through the profile's client keys and a release client stops on `TISLogoState` (`UI.DisableLogoState` is honoured only for `-debug` clients). Until that harness change lands (`-> X23`), wall-map I14's client half and anything that depends on an unguarded raise reaching a client stay **C**.
- (from 13) (14) **Compatibility constraint — a mod's `media/lua/server/` files execute in the MP client's Lua state too** (wall map I9, **M**, `x121` `phases.M7.mod_globals.client`), so every unguarded `server/` file runs **twice per MP session** and anything that mutates shared state, installs a wrapper or registers a handler does it twice. Resolve the side at runtime with a **nil-checked** `isServer()`, never from the folder. Mechanism still untraced `-> X18`. This is the rule every wrapper in a feasibility design (B2's eat wrapper first) has to be written against.
- (from 13) **`data/README.md` was stale on the merge rule; the correction landed at slice 13's close (2026-09-17).** It read "this dataset asserts no rule for how the two folders combine — the resolution order is still open question 1 in `docs/testing/profiles.md` § L0", but the rule has been **measured** since slice 11 and widened in slice 12 (the version dir's file wins a same-relative-path collision, `common/` supplies the rest, both in one Lua state; n = 2, plus the empty-version-dir arm) and that open question is **closed**. The consequence the ripple **(12, 13)** asked for is written in the map's I3 row — `signals` / `stats` / `top_events` describe the **live version folder only**, so a `common/`-heavy mod is under-read (for AutoCook: 7 of 10 Lua files, 1 238 of 2 155 lines, its only event registration, its only vanilla patch and its whole translation set), and a `media_at` that lists a `common/media` is the flag (177 of 230, swept 2026-09-10 17:47) — and `data/README.md` § mod-inventory now carries it too (2026-09-17): the merge rule is written as measured, `docs/testing/profiles.md` § L0 open question 1 is **closed**, and the under-read consequence is stated in its `common/media` bullet.
- (from 13) (14) **Check a bus command against the registration set, not against the prose.** The live harness registers **56 distinct names over 64 `TK.register(` call sites** (`grep -rn "TK.register(" testing/PZTestKit`, re-counted 2026-09-17; the 65th match is the definition in `PZTestKit_Core.lua:511`), and `docs/testing/README.md` § Command bus is a prose inventory that has trailed the harness before. Every experiment spec in the wall map was checked against that set: a command not in it is a **harness addition** under the standing rule — its own commit, the README inventory line, `luabalance.py`, the next acceptance run as its smoke test, landing **before** the session — not a call a driver may simply make. The six the map books are `drink.action` (X13), the `sandbox.set` route (X17), the `debug = false` launch flag (X23), `ctxmenu.create` with its `event.trigger` sibling (X26), `make_server(reuse=True)` (X28) and `craft.run` (X31).
